# 测试报告 · 打工 WORKING LIFE

## 1.7.0「超高」画质（2026-09-24）
- 自动化测试 **303 / 303 通过**；Web 导出 `play/wl-b97e8818.*`，44.2 MB（新增的都是着色器与代码，体积不变）。
- `tests/vis_shots.tscn`「超高」逐张检查：雨夜路面倒映霓虹与窗灯、车底与墙角的环境光遮蔽、4096 柔和阴影、夜间路灯光锥。
  调试中修正：① 乘法混合的遮蔽层被兼容渲染器二次色调映射，整个画面发黑 → 改为「黑色 + 透明度」混合；
  ② 只看深度的 SSAO 让掠射角的平地自己遮自己、近处路面发暗 → 改为由深度重建法线、只统计高出表面的采样；
  ③ 黄昏路灯光锥过亮像白色三角 → 亮度随夜色平方增长；④ 光锥被夜雾染成紫色 → 光锥不受雾影响。
- `tests/perf_stats.tscn`：「超高」典型视角绘制调用 731 / 644 / 553 / 462（四级阴影级联），「高」及以下与 1.6.0 相同。
- 无头 Chromium：桌面完整流程 0 错误、0 失败请求；手机竖屏（`?touch=1`）加载 0 错误；`validate_asset_versions.py` 通过。
- 未验证：真实显卡上的帧率（本环境只有软件渲染）；已加帧率保护，网页版「超高」持续低于 28 帧会自动降到「高」。

## 1.6.0 主角新形象（2026-09-24）
- 自动化测试 **303 / 303 通过**；Web 导出 `play/wl-1755db04.*`，44.2 MB。
- `tests/char_shots.tscn` 特写逐张检查：修正帽檐挡住眼睛、胡子过大过黑像张嘴。
- 无头 Chromium 桌面完整流程 0 错误、0 失败请求；`validate_asset_versions.py` 通过。

## 1.5.1 手机实测问题修复（2026-09-24）
- 自动化测试 **303 / 303 通过**；Web 导出 `play/wl-fff43c4f.*`，44.2 MB。
- 新增 `tests/touch_shots.tscn`（强制触屏模式按指定分辨率截图）：竖屏 540×1080、横屏 1080×500 各拍一张，
  HUD 面板与触屏按钮、摇杆互不重叠；叶片在逆光下呈绿色。
- 无头 Chromium：桌面 1280×720 完整流程 0 错误；手机竖屏（412×860、触屏、`?touch=1`）加载到主菜单 0 错误、0 失败请求。

## 1.5.0 画质工程阶段 2b（2026-09-24）
- 语法检查 **PARSE OK**；自动化测试 **303 / 303 通过**（`tools/build_web.sh`，退出码 0）；Web 导出 `play/wl-55094f91.*`，44.2 MB
  （照片贴图第一次以无损方式导入时是 55.0 MB，改为有损 WebP 导入后降到 44.2 MB）。
- 无头 Chromium 打开 `play/?v=55094f91`：26 秒启动，控制台 **0 错误**、**0 失败请求**；画面里是照片沥青、混凝土方砖人行道、新车模。
- `tests/perf_stats.tscn`：典型视角绘制调用 533 / 489 / 403 / 359；三角形 34～44 万（车模加细后一度到 53 万，车轮减面、快速路车流改用简化车模后回落）。
- 字体覆盖检查：游戏脚本与数据里所有显示的字符都在内置字体子集里（缺的 10 个字符都只出现在代码注释里）。
- 介绍页 360 / 768 / 1280 无横向溢出；`validate_asset_versions.py` 通过。

## 1.4.0 画质工程阶段 3（2026-09-24）
- 语法检查 **PARSE OK**；自动化测试 **303 / 303 通过**（`tools/build_web.sh`，退出码 0）；Web 导出 `play/wl-48889c26.*`，40.6 MB。
- `tests/perf_stats.tscn`：典型视角绘制调用 491 / 454 / 377 / 334；三角形 31～39 万。
- `tests/vis_shots.tscn` 新增 `aerial_dusk` / `aerial_night`（城外全景），与 `ref_dusk` 一起逐张目视检查；
  修正：远山三圈环只有中间一圈有高度，出现尖锥形山峰（改六圈、剖面平滑）。

## 1.3.0 画质工程阶段 2a（2026-09-24）
- 语法检查 **PARSE OK**；自动化测试 **303 / 303 通过**（`tools/build_web.sh`，退出码 0）；Web 导出 `play/wl-02d53344.*`，40.6 MB。
- `tests/perf_stats.tscn`：典型视角绘制调用 483 / 448 / 371 / 328；城市网格节点 393 个、顶点约 72 万；Label3D 141 个（路名牌 50 个，60 米外不渲染）。
- `tests/vis_shots.tscn` 新增 `edge_dusk`（城市外缘）、`cross_dusk`（路口）、`board_night`（夜间广告牌），逐张目视检查；
  修正：广告画面被背面剔除成黑色（改双面）；扇叶棕榈树冠过暗、枯叶裙过长（调亮、缩短、加大叶片）。

## 1.2.0 画质工程阶段 1（2026-09-24）
- 语法检查 **PARSE OK**；自动化测试 **303 / 303 通过**（`tools/build_web.sh` 内置全量测试，退出码 0）。
- Web 导出：`play/wl-ed431ec8.*`，共 40.6 MB（未引入新素材，体积不变）；介绍页「开始游戏」链接自动更新为 `play/?v=ed431ec8`。
- 无头 Chromium 打开 `play/?v=ed431ec8`：19 秒启动，完整走一遍主菜单到手机 / 地图 / 任务面板，控制台 **0 错误**、**0 失败请求**。
- `tests/perf_stats.tscn`：城市网格节点 375 个（1.1.0 为 560）；典型视角绘制调用 446 / 423 / 350 / 314。
  路灯光源池的点光源不增加绘制调用（关掉光源池前后测得相同）。
- `tests/vis_shots.tscn` 新增参考机位 `ref_dusk` / `ref_dusk_late`，逐张目视检查黄昏、正午、夜晚、雨夜；
  调试中修正：点光源衰减 1.4 时灯下地面只剩约 7% 亮度（改 0.9、能量 5）；黄昏雾色过亮把楼「漂白」（背光压暗）。
- 介绍页 360 / 768 / 1280 无横向溢出、图片全部加载（唯一失败请求是 AdSense，本环境网络策略拦截）；
  `validate_asset_versions.py` 通过；`git diff --check` 通过。
- 未验证：真实 GPU / 手机帧率（点光源在真实 GPU 上有像素开销，低画质不启用、触屏最多 4 盏）。

## 1.1.0 画质升级（2026-09-24）
同一环境（本环境没有真实 GPU，画面由 Mesa llvmpipe / SwiftShader 软件渲染）。
- 语法检查 **PARSE OK**；自动化测试 **303 / 303 通过**（`tools/build_web.sh` 内置的全量测试，退出码 0）。
- Web 导出成功：`play/wl-0b5ea7e4.*`，共 40.6 MB（引擎 wasm 与 1.0.0 相同）。
- 无头 Chromium 实际加载网页版：20 秒启动，主菜单 → 新游戏 → 开场 → 行走 → 手机 / 地图 / 任务面板，
  控制台 **0 错误**、**0 失败请求**；画面里能看到贴图立面、棕榈树、行驶与停放的汽车。
- `tests/perf_stats.tscn`：城市网格节点 560 个、顶点约 50 万；四个典型视角绘制调用 510 / 487 / 377 / 338
  （1.0.0 为 326～474；人物零件按关节合并后，基本抵消了新增的树、车和立面材质）。
- `tests/vis_shots.tscn` 与 `tests/site_shots.tscn`：黄昏 / 正午 / 夜晚 / 雨夜 × 航拍 / 观景台 / 街道截图逐张目视检查，
  无脚本错误；修正过的问题：自发光 ADD 运算导致夜里整栋楼发白、路面法线在低角度阳光下出现豹纹、
  镜头撞到树干突然拉近、夜间泛光过曝。
- 介绍页 360 / 768 / 1280 三档宽度无横向溢出，图片全部加载；唯一失败请求是 AdSense 脚本（本环境网络策略拦截，与本次无关）。
- `scripts/validate_asset_versions.py` 通过。
- 未验证：真实手机 / 独立显卡上的帧率；macOS 桌面版本次未重新导出。

---

# 测试报告 · 打工 WORKING LIFE 1.0.0

测试日期：2026-09-24 · 环境：Linux x86_64 容器，Godot 4.7.2 stable（官方二进制 + 官方导出模板），
Xvfb + Mesa llvmpipe（软件 OpenGL，用于截图），Playwright + Chromium（SwiftShader 软件 WebGL 2，用于网页版）。
本环境没有真实 GPU，也没有 macOS 真机；帧率数字只能说明「能跑」，不代表真实设备性能。

## 1. 语法与编译检查
`tools/run_tests.sh` 第一步加载工程里全部 .gd 脚本（自动加载就绪）：**PARSE OK**，0 个脚本错误。

## 2. 自动化测试：303 / 303 通过
命令：`GODOT=godot games/working-life/tools/run_tests.sh`（headless，看门狗 900 秒）。
21 个测试组：

| 组 | 覆盖内容 |
|---|---|
| startup | 启动到主菜单；23 个地点、17 个 NPC、导航网格、8 个工位、5 种住房的床与门全部生成 |
| data | 数据数量（地点 23、职业 8、技能 6、商品 39、NPC 17、任务 52、支线 35、事件 36、住房 5、结局 5、课程 18）与引用完整性 |
| economy | 收入、支出、现金不足自动刷卡、余额不足拒付、存取款、月统计、净资产、利息计入被动收入 |
| time | 日历、换日信号、跨月、周末、实时流逝、暂停菜单冻结时间 |
| skills | 升级经验、满级、上课（时间 +3 小时、每日一门）、读书、健身 |
| first_day | **真实按键流程**：走路 → 旅馆 → 前台开房 → 便利店按 E 买盒饭 → 人才市场申请 → 面试三题 → 录用 → 睡觉到次日 7 点 → 自动存档 → 便利店工位按 E 上班 → 收银小游戏 → 日薪到账 |
| job_payment | 日薪、绩效、迟到扣薪、旷工、请假不算旷工、升职、降职、辞职、解雇 |
| monthly_job | 程序员投简历 → 面试 → 月薪按班次累计 → 周五晚发到银行卡 → 周六休息 |
| housing | 旅馆到期、合租押一付一、月初扣租、搬家、欠租 7 天被请出 |
| investment | 开户条件、买入份额与成本、日波动、历史、冲击、卖出与已实现盈亏、债券计息、周期 |
| business | 创业条件、注册、招聘、产能、接项目并完成、营收 / 成本、权益计入净资产、分红、破产 |
| npc | 日程（柜台 / 下班回家）、按 E 对话、闲聊加关系、每日一次、送礼、直接录用、找钱包支线（生成 → 按 F 拾取 → 交付） |
| events | 36 个事件的**每一个选项**都执行一遍；强制触发、弹窗、冷却、延迟效果 |
| transport | 公交解锁、乘车扣费、耗时、到达；出租车目的地 |
| collapse | 健康归零住院；体力归零昏睡后恢复 |
| carry | 建筑工人 3D 搬运：时间暂停、按 F 扛起 / 放下、完成后结算日薪 |
| ui_windows | 手机 11 个 APP、地图 / 课程 / 设置 / 旅馆 / 衣柜 / 电脑 / 观景台 / 乘车窗口、5 个快捷键开关、Esc、F1、11 家商店、17 位 NPC 对话、7 种 2D 小游戏开始并结算 |
| navigation | 火车站 → 人才市场寻路 |
| save_load | 存到槽位 2 → 打乱状态 → 读回：现金、存款、日期时间、职业职位绩效、住房、关系、持仓、背包服装、技能、任务进度、剧情标记、天气、玩家位置 |
| endings | 五种结局条件逐一达成与推荐优先级 |
| journey | **完整流程**：主菜单 → 新游戏 → 第 1~7 章全部主线 → 观景台 → 播放结局 → 继续无限人生 → 时间继续流动 |

原始输出（节选，失败项为 0）：
```
== 导入资源
== 语法检查
== 自动化测试
== startup
== data
== economy
== time
== skills
== first_day
== job_payment
== monthly_job
== housing
== investment
== business
== npc
== events
== transport
== collapse
== carry
== ui_windows
== navigation
== save_load
== endings
== journey
ALL 303 CHECKS PASSED
== 导出 Web
== 整理到 play/
```

## 3. 实际运行（有渲染）
- Xvfb + OpenGL 3 实跑主场景并截图 19 张（主菜单、火车站夜景、白天街道、黄昏商务区、雨夜、公园、工地、观景台、便利店室内、
  旅馆房间、手机、地图、招聘、两种小游戏、投资 APP、结局画面）；Godot 输出无 SCRIPT ERROR；退出时无资源泄漏报告。
- 场景规模（`tests/perf_stats.gd`，软件渲染）：城市网格节点 335 个、顶点约 24 万、实时灯光 33 个（室内，远处淡出）、
  Label3D 91 个；四个典型视角每帧绘制调用 326~474（优化前 561~1153）。

## 4. 网页版（Chromium 无头 + 软件 WebGL 2）
- `play/` 加载约 12~15 秒后进入主菜单；**控制台 0 个错误，0 个失败请求**。
- 实际点击：新游戏 → 选存档位 1 → 开场列车镜头 → 跳过 → 进入火车站门口 → 按住 W 走路 → Tab 手机 → Esc → M 地图 → Esc → J 任务。
- 介绍页 `/games/working-life/` 在 360 / 768 / 1280 宽度下无横向溢出、无失败请求；游戏中心卡片显示正常。
- 站点检查：`scripts/validate_asset_versions.py` 通过（302 处引用）；`scripts/build_public_site.py` 产物包含介绍页、图片与 play/。
  该脚本本身在未改动的 HEAD 上同样以退出码 1 结束（缺少仅 CI 生成的 `.pro-build/rich-preview`），与本次无关。

## 5. 导出
- Web（无线程版）：成功，`play/wl-<哈希>.*` 共 40.5 MB（引擎 wasm 39.5 MB 与《工地搬砖》逐字节相同，Git 只存一份）。
- macOS：成功导出 `build/macos/WorkingLife.app`（Universal：arm64 + x86_64，ad-hoc 签名，166 MB）。未在 Mac 真机运行；
  `build/` 不入库。正式分发需要在 Mac 上 Developer ID 签名与公证。

## 6. 未验证 / 限制
- 未在真实 GPU、真实手机、macOS 真机上测帧率与触屏手感。
- 6~12 小时的通关时长是按数值估算的，没有真人完整通关计时。
- 自动化测试里「完整流程」为节省时间，用直接设置技能 / 资金的方式推进部分章节（每一步的系统本身在各自测试组里用真实流程验证过）。
