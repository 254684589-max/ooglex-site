# Ooglex 仓库开发规则

## 适用范围

本文件适用于仓库根目录及所有子目录。子目录可以增加更具体的 `AGENTS.md`，但不得放宽本文件中的数据真实性、密钥、安全、测试和生产部署规则。

## 项目背景

Ooglex 当前采用静态HTML、CSS和原生JavaScript前端，Python脚本生成各应用的 `data.json`，GitHub Actions负责定时更新，GitHub Pages承载主站，Cloudflare Workers承担部分动态代理。

“Ooglex AI金融终端”的建议入口为 `/apps/finance-terminal/`。优先复用现有应用和数据，不推倒重建。

开始工作前，先阅读：

1. `docs/PROJECT_BRIEF.md`
2. `docs/ROADMAP.md`
3. 与任务直接相关的现有页面、脚本、数据和工作流
4. `CHANGELOG.md`

动全球产业链板块（`apps/supply-chain/`）之前，另读：

- `docs/SUPPLY_CHAIN_BACKLOG.md` —— 当前状态与待办（这是台账，每次推进后更新）
- `.claude/skills/supply-chain/SKILL.md` —— 开发口径、踩过的坑、四道校验与收尾
- `docs/SUPPLY_CHAIN_GRAPH.md` / `docs/SUPPLY_CHAIN_SOURCES.md` —— 历史实测结论与许可红线

该板块的第一条规则：**框架可以用行业通识建，公司间的关系必须有可核验的原始出处。**
模型的行业知识不是数据源。

## 不可违反的规则

1. 保留现有网站功能、URL和数据管道。未经明确要求，不删除、重命名或替换现有栏目。
2. 中文界面优先；关键金融术语可同时显示英文。
3. 手机和电脑必须都能正常使用，不得只按桌面宽屏设计。
4. 优先兼容Cloudflare Pages、Workers、D1、R2，但不得无明确需求就迁移或增加服务。
5. API密钥、令牌、账号和其他秘密绝不能写入前端、提交到GitHub或输出到日志。
6. 金融数据必须显示来源和更新时间；能提供时还应显示数据日期、频率和延迟状态。
7. 不得伪造实时行情，不得把日频、延迟或静态数据标成实时。
8. 接入真实API前可以使用模拟数据，但必须在数据和界面中显著标注。
9. 每次只完成一个可独立验收的模块，避免夹带无关重构。
10. 修改后必须运行所有适用的构建、测试和检查。
11. 未经项目所有者明确同意，不得合并或推送到生产分支，不得触发生产部署。
12. 不得擅自修改GitHub Secrets、Cloudflare Secrets、域名、账单、访问控制或生产数据库。
13. 不得删除有效历史数据来掩盖抓取失败；失败时优先保留上一份有效数据并显示状态。
14. 所有修改都必须可审阅、可解释、可回退。

## 任务工作流程

### 开始前

- 确认当前仓库、分支和工作区状态。
- 检查是否存在用户未提交或与任务无关的改动；不得覆盖。
- 明确本次目标、允许修改的文件、禁止事项和完成标准。
- 复杂任务先写计划；不清楚的产品或数据口径先询问。
- 不自动安装新依赖。确有必要时先说明原因、成本和替代方案。

### 开发中

- 优先小范围修改和复用现有实现。
- 保持现有页面和 `data.json` 向后兼容。
- 不在同一任务中同时重构多个无关应用。
- 不复制现有取数脚本来制造第二套事实来源。
- 外部新闻、AI文本和接口字段视为不可信输入。
- 渲染外部文本优先使用 `textContent`；使用 `innerHTML` 时必须先可靠转义或消毒。
- 网络失败、字段缺失和空数据必须有明确的用户可见状态。
- 不用空文件、默认零值或模拟值静默覆盖有效数据。

### 完成后

- 检查差异，只保留本次任务文件。
- 运行适用的检查并记录实际命令和结果。
- 更新 `CHANGELOG.md`。
- 列出新增、修改和删除的文件。
- 说明未运行的检查及原因。
- 未经明确批准，不合并到 `main`，不执行部署命令，不修改生产配置。

## 金融数据规范

新建或修改金融数据时，应尽可能提供以下元数据，同时保持旧消费者兼容：

| 字段 | 要求 |
|---|---|
| `source` | 数据提供方名称；能提供时附官方链接 |
| `asOf` | 数据实际对应日期或时点 |
| `updatedAt` | 文件或接口更新时间，优先使用UTC ISO 8601 |
| `frequency` | `realtime`、`delayed`、`daily`、`weekly`、`monthly`、`quarterly`或`annual` |
| `status` | `ok`、`partial`、`stale`、`error`或等价状态 |
| `demo` | 模拟数据为 `true`，真实数据为 `false`或省略 |
| `note` | 代理指标、估算方法、延迟和限制说明 |

补充规则：

- “实时”仅用于数据源明确提供实时行情且页面实际按相应频率刷新的情况。
- 使用ETF、期货、比价或人工估算代理另一个指标时，必须说明代理关系。
- 不同频率的数据不可在未说明的情况下直接比较。
- 计算值必须可从输入字段和公开公式复现。
- AI生成文字不能作为价格、财务指标或新闻事实的数据源。
- 数据过期阈值应结合频率配置，不应对所有来源使用同一个固定天数。
- 数据源全部失败时，保留上一份有效数据并把状态标记为过期或失败。

## 模拟数据规范

- 模拟文件必须包含 `demo: true` 或等价字段。
- 页面首屏必须显示“模拟数据”或“演示数据”，不能只写在页脚。
- 模拟值不得使用容易被误认为当前真实行情的精确数值。
- 接入真实数据后，应删除不再使用的模拟路径或明确保留为开发夹具。
- 测试夹具不得被生产页面误加载。

## 前端与体验规范

- 以中文为默认语言，避免生硬机翻。
- 最低检查代表性宽度：360px、768px、1280px。
- 不允许关键内容横向溢出、按钮被遮挡或只能悬停操作。
- 交互控件应可用键盘访问，并有可见焦点状态。
- 尊重 `prefers-reduced-motion`。
- 颜色不是表达涨跌、风险和状态的唯一方式，应同时使用文字、符号或标签。
- 图表必须提供单位、时间范围和数据说明。
- 加载、空数据、部分数据、过期和错误状态必须区分。
- 优先使用同源、本地化资源；新增第三方脚本前评估隐私、性能和可用性。
- 不得因新终端改动破坏现有页面的SEO、canonical、分享信息或导航。

## Cloudflare与后端规范

- 第一阶段MVP保持静态优先，不把Cloudflare迁移作为前置条件。
- Workers用于需要隐藏密钥、跨域代理、缓存、限流或服务端转换的接口。
- D1只用于确有持久化需求的数据，例如自选股、组合、偏好和提醒。
- R2只用于历史快照、大文件或不适合进入Git仓库的静态资产。
- 所有动态能力必须有超时、限流、错误状态和可接受的静态降级。
- Worker密钥使用Cloudflare Secrets；GitHub任务密钥使用GitHub Actions Secrets。
- 不在配置样例中填入真实ID、密钥或个人令牌。
- 新服务必须记录用途、费用风险、删除方法和故障回退方案。

## 测试与验证

仓库当前没有统一的前端构建系统，因此“必须运行构建和测试”按任务适用范围执行：

- 修改Python：至少运行对应测试（如有）及 `python -m py_compile <changed-files>`。
- 修改JSON：至少运行JSON解析校验，并检查必需字段和数据状态。
- 修改HTML/CSS/JS：运行现有检查（如有），并进行目标页面冒烟测试。
- 修改响应式界面：检查360px、768px和1280px代表性宽度。
- 修改数据加载：覆盖成功、空数据、部分缺失、过期、模拟和请求失败状态。
- 修改GitHub Actions：检查YAML语法、权限范围、并发和失败回退。
- 修改Worker：检查允许来源、方法、超时、密钥隔离、限流和错误响应。
- 修改链接或路径：检查现有入口、相对路径、canonical与返回链接。
- 所有任务都运行 `git diff --check`（本地Git可用时）。

如果静态页面没有构建步骤，应明确记录“构建：不适用（纯静态）”，但仍必须执行相关语法、数据和页面检查。工具或依赖不可用时，不得声称测试通过；应说明未验证项。

## Git与发布规范

- 从默认分支开始工作时，使用独立功能分支。
- 提交中只包含本次任务文件。
- 自动生成的数据文件只有在任务明确要求更新数据时才能提交。
- 不得为了通过检查而修改无关数据或降低验证标准。
- 未经项目所有者明确同意：
  - 不直接更新 `main`
  - 不合并Pull Request
  - 不运行生产部署
  - 不修改生产域名、Worker路由、D1或R2
- 发布前必须说明目标分支、改动范围、检查结果和回退方式。

## CHANGELOG规范

每个完成的模块都应更新根目录 `CHANGELOG.md`：

- 使用 `YYYY-MM-DD` 日期。
- 按“新增、修改、修复、数据、文档、安全、移除”归类。
- 记录用户可感知的变化和重要内部规则。
- 不把计划中的功能写成已经完成。
- 明确记录是否部署；默认写“未部署”。

## 完成定义

只有同时满足以下条件，任务才算完成：

- 范围内功能或文档已经实现
- 原有相关功能未被破坏
- 数据来源、时间和状态符合规范
- 适用的构建、测试和检查已经运行
- `CHANGELOG.md` 已更新
- 修改文件和验证结果已列出
- 已知限制已说明
- 没有密钥或敏感信息进入代码、日志或提交
- 没有未经批准的生产部署
