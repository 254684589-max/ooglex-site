# Ooglex AI金融终端：开发路线图

> 路线图采用“小步、可验收、可回退”的方式推进。相关度高的功能可以按2～4项组成一个完整模块，但每项仍需独立提交和验收。

## 总览

| 阶段 | 目标 | 状态 |
|---|---|---|
| 0 | 项目基线与长期规则 | 已完成 |
| 1 | 静态金融终端MVP | 已完成 |
| 2 | 数据标准与质量保障 | 进行中 |
| 3 | Cloudflare后端能力 | 未开始 |
| 4 | AI研究助手 | 未开始 |
| 5 | 自选股与投资组合 | 未开始 |
| 6 | 运营、合规与规模化 | 未开始 |

## 当前实现快照（2026-08-21）

- 稳定V1开发已从包含既有开发成果的最新线上`main`生产快照继续，开发分支前序提交是该快照的祖先；本批不回写`main`、不发布、不部署，也不改动行情与健康JSON。
- 首屏分区加载新增真实绘制边界：5份关键来源完成渲染后等待双`requestAnimationFrame`，再启动首屏以下分区调度；TradingView宿主监控仍与绘制并行，不因延迟请求而推迟。无`requestAnimationFrame`环境使用明确降级策略，不伪造时间指标。
- 延迟加载改为分区故障隔离：风险、研究、资讯、运行证据任一分区失败时，其余分区继续加载并渲染；页面总状态显示`partial`，失败分区保留独立错误提示，不再把单点失败升级为整页失败。
- 三档Chrome与离线契约新增确定性分区证据：核对关键请求集合、绘制/调度顺序、实际分区启动顺序、逐请求状态、零重复网络请求及四分区`loading → ready`转换。证据不读取或保存行情值，并继续受14KB生产加载器和15KB回归探针预算约束。
- 市场状态、市场研究与事件资讯的视图代码已拆为三个原生按需模块：首屏及只打开其他分区时保持`idle`，对应资源组完成后才导入并渲染；异步模块异常沿用单分区失败隔离。`app.js`由215,878字节降至195,262字节，和13,807字节加载器合计209,069字节，继续低于230KB常规加载预算；三个延迟模块各自受6KB、14KB与10KB预算守卫。
- 已完成金融终端页面骨架、首页入口、8项核心资产卡片、统一加载/过期/错误状态及360/768/1280像素响应式规则。
- 已完成首轮长页与可读性热修复：密集辅助文字计算字号不低于11px，顶部改为动态来源状态计数，新增7项粘性分区导航，数据方法说明改为摘要加可展开详情，页脚统一为`Public Beta`；移动端导航与法律入口由真实Chrome守卫至少44×44px触控区域。
- 已完成首轮前端性能拆分：`app.js`由229,081字节降至不超过220KB的生产入口预算，请求缓存与分区调度进入原生模块，浏览器回归探针仅在`regression=1`时动态加载。普通访问先读取1份配置和5份首屏资源，另外13份资源在市场状态、研究、资讯或运行证据接近视口及点击导航时按需加载；跨分区共享资源只请求一次，回归模式仍主动加载全页。
- 已补齐CoinGecko公开产品页面契约：页脚常驻使用条款与隐私政策入口，明确CoinGecko所有权、归属、禁止转售/绕过、责任排除、加密资产波动、静态页面数据处理和第三方组件边界；页面无新增追踪脚本。
- 8项核心资产已取消全部演示数值：FRED `DGS10`、FRED `DTWEXBGS`、EIA `RWTC`与资产榜CoinGecko/Yahoo `BTC/USD`由站内日更管道生成；三大指数与黄金使用TradingView官方免费组件展示`SPY`、`QQQ`、`DIA`与`GLD`，逐卡明确标为`SPX`、`NDX`、`DJIA`与`LBMA Gold Price PM`的ETF代理，不保存或导出组件原始行情。
- 四项TradingView代理新增逐卡运行时状态：初始明确显示加载中，8秒内验证`tv-mini-chart`完成Custom Element注册，再检查每个组件元素已连接、已定义且具有非空布局；任一卡失败只显示官方标的链接且不回填数值，脚本晚到时自动恢复。宿主挂载不冒充报价已渲染、行情新鲜或市场开市，完整状态由同一离线与浏览器回归契约覆盖。
- 四项代理卡的失败回退现区分加载超时、组件注册失败、当前浏览器无法验证与宿主挂载异常；用户仍可逐卡访问官方来源，页面不把技术原因改写成行情不可用或数据过期。
- 三档Chrome资格检查现使用与生产页面一致的完整8秒窗口并输出四项代理的机器可读运行证据：逐视口仅保存代理代码、`mounted / unavailable`状态、受控原因和截图文件名，拒绝价格字段及伪造渲染/新鲜度结论；JSON保留14天、截图保留7天，不进入Git历史，也不虚增核心管道日更周期。150毫秒快速回归结果不得冒充稳定V1运行证据。
- 代理运行证据新增白名单脚本传输层：逐视口区分请求未观察、响应等待、HTTP成功、HTTP失败与浏览器阻断，并只保存固定脚本URL、状态码及缓存布尔值；不采集响应正文、任意日志、其他请求或组件报价。
- 脚本传输与组件宿主证据现生成可复算的关联诊断及Markdown运维摘要：区分传输失败、注册超时、浏览器运行时限制、部分挂载与宿主验证失败；该诊断不宣称报价渲染、新鲜度或开市状态。
- 免费代理运行证据新增最多7周期的21:00 UTC去重趋势与两周期运维评估：连续两周期脚本加载且宿主全挂载才为`HEALTHY`，单周期、部分挂载或首次恢复为`WATCH`，连续脚本失败或全回退为`WARN`，收集不完整为`UNKNOWN`；该评估不读取行情，也不虚增核心管道资格周期。
- 稳定V1资格流程在四条核心数据管道完成后，使用最新开发分支的最终三档Chrome证据重建同一代理趋势和运维评估，并把JSON、Markdown及截图作为14天Artifact保留；资格运行自身不成为趋势历史源，不会重复计算同周期。
- 四辅助来源资格流程在CNN、OFR、Forex Factory与Google News/Yahoo刷新后，也用最终页面生成同格式代理趋势和评估；辅助运行只证明刷新后页面契约未回退，不作为质量趋势历史源，也不扩大核心数据门槛。
- 已完成“市场状态”模块：宏观雷达制度信号、CNN恐慌与贪婪指数、OFR金融压力指数；三源独立加载，不生成口径不明的合成总分。
- 已完成“市场强弱与领袖”模块：28项跨资产五周期强弱、250项全球资产市值前五及500家公司中的上市公司市值前三与当日领涨/领跌；三源独立加载，慢变量估算、代理标的和未上市估值均明确标注。
- 跨资产中证500保留Yahoo原指数`000905.SS`为首选，并登记人民币`510500.SS`中证500ETF价格收益率回退；只有原指数数据点不足或异常时才启用，页面显式显示`PROXY`及跟踪误差，不静默冒充指数精确收益。
- 跨资产沪深300继续优先使用Yahoo原指数`000300.SS`，并登记人民币`510300.SS`沪深300ETF价格收益率作为同口径预防性回退；直接指数正常时不会切换代理。
- 已接入经济日历信息流：复用Forex Factory公开周历，展示接下来4项高、中影响事件及实际、预测、前值；按36小时和当前周范围判断过期，加载失败不影响其他模块。
- 已接入财经新闻信息流：只读取Google News聚合的市场板块并展示最新5条，按12小时文件时效和36小时文章时效判断过期；同文件Yahoo行情快照不参与终端行情。
- 已建立逐资产/逐信号数据适配、结构校验、新鲜度判断、失败回退和敏感信息检查；BTC/USD复用现有免密钥资产榜管道，CoinGecko主路径、Yahoo不同涨跌口径降级及历史保留均明确显示；四项ETF代理不需要Ooglex行情API密钥，时效与市场状态由TradingView组件标示，组件失败时只显示官方标的链接。
- 阶段1的规划数据模块、全页面键盘与读屏语义、触屏尺寸、减少动画/高对比规则均已完成；新增无前端依赖的Chrome回归脚本与只读CI，自动检查360/768/1280像素布局、溢出、数据加载、触控目标、标签页键盘操作和截图。
- 阶段2已为`asset-tracker`、`companies`与`asset-ranking`三条聚合管道补齐向后兼容的逐条行情、回退、估值、待确认、来源和时间契约；旧快照不能证明的数据不会被追认为实时。
- 三条聚合管道已新增独立`health.json`：跟踪发布覆盖、最近任务行情覆盖、最后尝试与成功时间、连续整批失败、快照保留和实际恢复顺序；金融终端显示健康摘要并在72小时后标记报告过期，只读CI保留统一诊断产物。首次迁移无法追溯的失败次数明确为未知。
- 公司榜与资产榜健康现已把动态行情、已登记市值代理和慢频估值分层：450/235项动态覆盖仍独立检查，50/15项慢频估值显示单独覆盖；合法慢变量不再误报为行情失败，任何动态回退、未知、不可用或未登记`partial`路径仍会降级并进入门禁。
- 三条聚合更新任务已完成冲突与仓库增长治理：各自使用独立并发锁，生成前同步远端分支，只能暂存明确拥有的数据/健康/Logo路径，无差异时跳过提交；逐次诊断仅以14天Artifact保留，不进入Git历史。
- 已完成Beta远端运行验收与上线门禁代码：统一核对宏观雷达三项官方序列、BTC/USD逐条行情、四项免费代理契约、三条聚合健康快照和四条GitHub Actions日更证据，输出Beta/稳定V1两套`PASS / WARN / BLOCKED`结论；报告只读并以14天Artifact保留。四项核心行情许可阻塞现已由合规免费代理消除，剩余稳定V1阻塞是四条工作流继续累计至7个独立日更周期。
- 宏观雷达已为FRED `DGS10`、FRED `DTWEXBGS`与EIA `RWTC`建立独立`health.json`：逐源跟踪本轮成功/回退/不可用、最后尝试与成功、连续失败和旧值保留；旧历史不可追溯时明确为`unknown`。宏观任务现与三条聚合任务使用同一并发、同步、精确路径和短期Artifact治理，Beta门禁同时检查观测值与更新链证据。
- 四张真实行情卡片已分别接入逐源`UPDATE HEALTH`：三项宏观卡交叉校验序列ID、来源、观测日与成功更新时间，BTC/USD再核对CoinGecko、Yahoo降级或历史回退的来源计数与同批成功时间；单源健康异常只降级对应健康栏，不隐藏仍可验证的行情，也不拖累其他来源。
- 三项官方行情已建立最多8点的最近观测窗口：`DGS10`按bp、`DTWEXBGS`与`RWTC`按百分比显示窗口区间变化；成功刷新重建窗口，失败完整保留旧窗口，旧快照无法证明的日期不补造。RWTC最新观测已恢复到2026-08-11；各来源仍按自身美国工作日阈值动态判断`STALE`。
- 金融终端“稳定V1运行证据”同屏读取`macro-radar`、`asset-tracker`、`companies`与`asset-ranking`四份健康快照及一份门禁证据快照：每卡分层展示更新健康、连续成功周期、最近周期和可复核运行链接；2026-08-20远端资格复核后四条管道均为5/7，距离稳定V1各剩2个不同UTC日成功周期；证据超过72小时显示`STALE`，结构或来源无效显示`UNKNOWN`，不会把同周期重跑重复计数。
- 已新增只收集公开复核信息的数据问题Issue表单与四管道故障恢复手册；表单禁止提交密钥、令牌、账号或持仓，手册固定公司榜先于资产榜的恢复顺序、14天诊断证据和禁止强推/伪造时间戳规则。
- CNN恐慌与贪婪、OFR、Forex Factory经济日历和Google News市场资讯新增独立`health.json`与统一组件健康契约；旧运行历史从`unknown`开始，页面把内容数据日和12/36/72小时更新链证据分开显示。四条辅助任务已补齐独立并发、生成前同步、精确路径所有权、失败证据先提交和14天诊断，且不会扩大四条核心行情管道的Beta门禁口径。
- 已新增稳定V1四管道开发分支资格验收：宏观雷达、跨资产和公司榜先独立运行，公司榜成功后再运行资产榜；随后从最新分支执行完整质量、浏览器和Beta/稳定V1门禁。流程硬阻断`main`，只保留7至14天诊断证据，不合并、不部署。
- 已新增并完成CNN Fear & Greed与OFR辅助来源开发分支资格闭环：两源先同时启动、单源失败不取消另一源、旧运行按ID基线排除；2026-08-12首次远端运行后，CNN为1/1组件、OFR为5/5组件刷新，均由迁移`unknown`转为真实`tracked/healthy`。该流程不扩大或替代核心四管道的3/7周期门槛。
- Forex Factory经济日历与Google News/Yahoo资讯快照已纳入同一开发分支资格编排：四条辅助任务全部先启动再独立收集结论，一条失败不取消其他来源。2026-08-12首次四源远端闭环为`4/4 success`：CNN 1/1、OFR 5/5、经济日历1/1、资讯快照6/6组件均为`tracked/healthy`，并通过刷新后完整质量与三档响应式检查。
- 中证500回退已在远端真实取数中验证：Yahoo原指数`000905.SS`数据点不足时发布人民币中证500ETF `510500.SS`，页面与逐条元数据显式标记`PROXY`、价格收益口径和跟踪误差；同批沪深300原指数`000300.SS`正常，未误切换至预防性ETF回退。
- 稳定V1资格工作流现会在资产取数、逐条质量、来源健康、资产工作流或金融终端适配契约变化时自动运行；刷新后的聚合状态测试不再写死某次生产快照的`partial/healthy`状态。最终远端资格运行的四管道、完整质量、360/768/1280像素Chrome、readiness、来源健康与截图Artifact全部成功，未合并、未部署。
- 每日调度器查询最近手动运行时已按当前分支隔离，开发资格运行不会让`main`误判生产窗口已触发；DGS10异常夹具改为相对当前快照动态偏移1bp，日更数值推进不再制造假失败，真实窗口一致性阈值未放宽。
- 2026-08-13项目所有者改为免费优先：暂停`SPX`、`NDX`、`DJIA`与`LBMA Gold Price PM`精确基准采购，选定`SPY`、`QQQ`、`DIA`与`GLD`显式ETF代理。机器门禁拒绝代理冒充原标的、抓取/保存组件行情、引入收费源或缺失TradingView官方来源；询价材料保留为未来升级精确基准的备用文档。
- 稳定V1观察新增开发分支跨日周期标记：只在21:00 UTC进入新周期后允许更新，同周期重复运行不写入；标记仅触发四管道资格流程，实际1/7至7/7仍由远端`workflow_dispatch`记录复算，不合并、不部署。

## 阶段0：项目基线与长期规则

### 目标

在不开发业务功能的前提下，明确定位、范围、质量规则和开发流程。

### 交付物

- `docs/PROJECT_BRIEF.md`
- `docs/ROADMAP.md`
- `AGENTS.md`
- `CHANGELOG.md`

### 验收标准

- 四份文件存在且内容互不冲突
- 明确数据真实性、密钥、测试、移动端和部署规则
- 不修改现有页面、脚本、数据和工作流
- 不部署生产环境

## 阶段1：静态金融终端MVP

### 目标

在 `/apps/finance-terminal/` 建立统一终端，直接读取现有 `data.json`，不新增外部数据源。

### 建议拆分顺序

每一项单独开发、测试和验收：

1. 页面骨架、导航和响应式栅格
2. 数据加载器与统一错误状态
3. 顶部行情条
4. 宏观状态卡
5. 跨资产表现模块
6. 风险与情绪模块
7. 经济日历模块
8. 财经新闻模块
9. 公司排行模块
10. 数据来源、频率和过期提示
11. 移动端与无障碍优化
12. 全页面冒烟测试与回归检查

### 验收标准

- 原有栏目不受影响
- 不复制已有取数脚本
- 数据来源、日期、频率和状态可见
- 模拟、过期和加载失败状态不会伪装成实时数据
- 360px、768px和1280px等代表性宽度布局正常
- 键盘操作、焦点状态和减少动画偏好可用
- 所有可运行的检查通过
- 未经批准不部署生产环境

## 阶段2：数据标准与质量保障

### 目标

降低免费接口不稳定、字段不一致和数据过期造成的风险。

### 计划

- 建立终端数据适配层，避免页面直接依赖各栏目所有内部字段
- 为新数据定义最小元数据：`source`、`asOf`、`updatedAt`、`frequency`、`demo`、`status`
- 保持现有 `data.json` 向后兼容
- 增加JSON结构校验和异常值检查
- 增加数据新鲜度检查
- 增加内部链接和关键页面冒烟测试
- 记录各数据源的更新频率、许可和替代源
- 将失败回退状态明确传递到前端
- 为官方序列保存有界最近观测窗口，并校验日期顺序、末值、单位和失败保留
- 评估减少自动提交冲突和仓库膨胀的方法

### 验收标准

- 错误数据不会静默覆盖有效数据
- 过期数据可以被机器和界面识别
- 数据源改变结构时能尽早失败并报警
- 数据脚本、JSON和关键页面具有可重复检查方法

## 阶段3：Cloudflare后端能力

### 目标

仅在静态架构无法满足明确需求时，逐步引入Cloudflare服务。

### 分工建议

- **Pages**：是否迁移主站需单独评估，不作为MVP前置条件
- **Workers**：统一API网关、AI代理、限流、缓存和数据转换
- **D1**：自选股、投资组合、用户偏好、提醒规则
- **R2**：历史数据快照、研究附件和大体积静态资产
- **KV/Cache**：短期缓存、配额和轻量配置

### 原则

- 每引入一个服务，必须先写清楚解决的问题、成本和回退方案
- 密钥只放在GitHub Secrets或Cloudflare Secrets
- 不允许为了“技术升级”而无目的迁移
- Cloudflare故障时，公开静态数据页面应尽可能继续可用

## 阶段4：AI研究助手

### 目标

让AI解释站内已有数据，而不是凭空生成市场事实。

### 计划

- 只允许AI读取带来源和时间戳的数据
- 回答中标明使用的数据日期和来源
- 对过期、缺失或冲突数据明确提示
- 区分事实、计算结果和模型推断
- 增加提示注入防护、限流和成本控制
- 为常见研究问题建立可重复评测
- 不输出保证收益、确定性买卖指令或伪造新闻

### 验收标准

- 关键结论可以追溯到具体数据
- AI不可把模拟数据描述为真实行情
- API密钥不进入前端、日志或仓库
- 达到预设的回答准确性和成本上限

## 阶段5：自选股与投资组合

### 目标

提供个人研究记录，而非券商交易功能。

### 计划

- 第一版可先采用本地存储，并明确数据只保存在设备
- 需要跨设备同步时再引入账号和D1
- 支持自选股、持仓记录、成本、收益和资产暴露
- 增加导入、导出和删除个人数据能力
- 明确隐私政策与数据保留期限
- 不接入券商下单

## 阶段6：运营、合规与规模化

### 目标

把个人项目提升为可持续维护的公开产品。

### 计划

- 核查外部数据授权和商业使用条款
- 完善隐私政策、服务说明和免责声明
- 建立错误监控、数据源健康检查和恢复流程
- 评估API、Workers、D1、R2和AI成本
- 建立性能预算、可访问性检查和安全复核
- 确定项目许可证
- 只有在明确批准后才执行生产迁移或正式发布

## 每日开发节奏

每个开发任务应按以下顺序进行：

1. 从路线图选择一个最小独立模块
2. 写清目标、涉及文件、限制和完成标准
3. 检查相关现有代码和数据结构
4. 只修改任务范围内的文件
5. 运行相关构建、测试和静态检查
6. 手动检查手机与电脑关键状态
7. 更新 `CHANGELOG.md`
8. 列出修改文件、检查结果和已知限制
9. 等待审阅；未经明确同意不部署生产环境

## 近期下一步

当前下一批建议为：

> 本批已经把市场状态、研究排行和事件资讯三个屏下视图改为独立按需模块，并以首屏零模块请求、单分区请求隔离、文件预算和异步渲染屏障守卫。下一步优先继续累计宏观雷达、跨资产、公司榜和资产榜剩余2个不同UTC日成功周期；同周期重跑不重复计数，并结合机器可读浏览器证据与三档截图观察TradingView四项免费组件。下一工程批次只在不干扰7周期观察时拆分运行证据视图或高内聚适配层，继续守住首屏请求、模块失败隔离、分区顺序和真实浏览器证据。只有未来明确要求恢复精确指数/黄金基准时，才重新启动三组付费授权询价。继续只推完整开发分支，不合并、不部署。
