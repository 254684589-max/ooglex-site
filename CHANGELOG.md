# 更新日志

本文件记录Ooglex网站和“Ooglex AI金融终端”的重要变化。

记录规则：

- 使用 `YYYY-MM-DD` 日期。
- 按“新增、修改、修复、数据、文档、安全、移除”分类。
- 只记录已经完成的工作，不把路线图计划写成完成项。
- 每个开发任务注明是否已经部署；默认不得部署。

## [未发布]

### 新增

- 2026-08-28，**品类行情独立成应用：「全球市场行情 / Global Markets」**（`apps/markets/`），并进首页与数据中心。
  - **名字**：中文「全球市场行情」，英文 `GLOBAL MARKETS`。它与「大类资产收益率」不是一回事——后者比的是各品类的收益率排名，这一页是逐个标的的报价与走势，口径不同、互不替代，因此各自成页。
  - **一份代码两页共用**：新页直接复用金融终端那份数据层与视图（`finance-terminal-board-data.mjs` / `-board-view.mjs`），六份数据文件的相对深度相同，不需要第二套取数逻辑，也不会出现两套口径。行情板样式抽成共享的 `terminal-board.css`，金融终端「专业模式」与本页长得完全一样。
  - **逐行链接跨目录可用**：`quoteHref` 增加 base 参数，由页面用 `data-quote-base` 声明详情页位置，两页的行都能正确指向同一个 `quote.html`。
  - **页面自己的统计条**：标的总数、品类数、数据日区间、今日涨跌家数与数据源家数，全部由本页已经画出来的行汇总，不引入第二套口径；数据日不一致时给区间而不是只写最新那天。
  - **首页**：数据列表新增 `01 全球市场行情`（原 01–19 顺延为 02–20），首页应用计数 16+ → 17+，中英文案同步。**数据中心**：新增同名卡片，预览读它体量最大的那条管道（跨资产），并写明「另有公司榜、加密与美债曲线」，卡片链接指向新页面——数据中心的卡片首次支持「链接目录」与「预览数据目录」分开。站点地图同步收录。
  - **入口互链**：金融终端的品类行情说明里给出新页面入口；行情详情页顶部同时保留「全球市场行情」与「金融终端」两个返回入口。
  - 360 / 768 / 1280 三档实测新页与详情页均无横向溢出；金融终端三档浏览器回归通过，离线校验、质量、健康、治理与发布门禁全绿。
  - **已部署**（2026-08-28，经所有者确认后合并 `main`，`Finance Terminal Quality` #162 全绿；GitHub Pages 由 `main` 直接提供服务）。

- 2026-08-27，**「专业终端」入口卡预览接入真实全球指数涨跌**。逐块审查效果图后确认：底部三张卡的预览是最后一处大差距——效果图是真实数据表，我们是空条。
  - **先纠正一个此前的错判**：我之前说这三张预览「只能是装饰底图，因为品类行情与经济日历属于延迟加载分区」。实测首屏其实已经取了 11 份 JSON，其中就有跨资产管道 `asset-tracker`（56 个标的、含当日涨跌），地缘风险定价一直在用它。所以指数预览完全可以上真值，**不新增任何请求**——加载器按资源键去重。
  - **新增 `finance-terminal-gateway-preview.mjs`**：按需导入，只镜像已校验的当日涨跌，与品类行情板同源、不另算口径。逐行只收 `dataMeta.mode=market` 且 `status=ok`、未过期、当日涨跌可用的标的；任一条件不满足就不进表，宁可少几行也不推算补齐。
  - **代理标的不进这张表**：如以 ETF 代理原指数的沪深300，这里没有位置写清代理关系，未加说明地展示会把代理当成原标的。它们在品类行情板里有完整披露。
  - **数据日给区间不给最新值**：各行数据日不一致时表头写 `08-26~08-27`，只写最新的一天会把整张表说得比实际更新。
  - **涨跌不只靠颜色**：正负号与颜色同时给出；逐行 `title` 带该行自己的来源与数据日。
  - **无障碍随之修正**：预览容器原本整块 `aria-hidden`（纯装饰）。显示真值后，数据表改为对辅助技术可见并带标签，同卡里的走势图与强弱条仍保持 `aria-hidden`。
  - **状态齐全**：跨资产管道失败时表头写「跨资产管道不可用」、各行留空，不装作有数据（已实测 `asset-tracker` 返回 500 的情况）。
  - 新增离线契约测试（代理/过期/降级/缺涨跌条目一律不进表、数据日区间、正负号格式）；离线校验 81 项全绿；360 / 768 / 1280 / 1440 / 1672 五档实测通过，另跑了复刻 CI 断言的可读性探针；CI 浏览器回归在 360 / 768 / 1280 三档通过。**已部署**（2026-08-27，经所有者确认后合并 `main`，`Finance Terminal Quality` run 33111775914 全绿；GitHub Pages 由 `main` 直接提供服务）。

- 2026-08-27，**首页与参考效果图第三轮对齐：字体与读数措辞**。这轮按要求把效果图的元素逐块过了一遍，抓到的主因是排版而不是布局。
  - **读数改用比例无衬线体**：效果图里 `5.4` / `4.2` / `5,321.41` / `-0.32%` 全是比例字体，我们一律用等宽体，字距被拉宽，整体气质偏「终端」而不是「仪表盘」——这是看上去差距大的主因。总览首屏的 HUD 读数、证据条与行情带的名称和数字改为 `var(--sans)` 并开 `tabular-nums` 保持列对齐；`RECENT OBSERVATIONS` 一类微型技术标签仍是等宽。品牌字距同步收紧一档。
  - **风险雷达等级改中文优先**：`LOW RISK / MODERATE / HIGH RISK` 此前只有英文，既不符合「中文界面优先」，也与效果图的「中等」对不上。改为「低风险 · LOW RISK」等中英并列，并给出短态 `data-short-state`。
  - **首屏 HUD 只取短标签**：极光首页同步模块新增短态优先取值——优先用来源节点声明的短态，其次取「中文 · ENGLISH」的中文段。首屏现在读作「中性 / 中等 / 偏低」，与效果图一致；完整判语（如「低于历史平均压力」）在「市场状态」卡片里原样保留。OFR 金融压力的短态由同一个正负比较派生，不引入新的判断口径。
  - **行情带走势图加高**：走势图从约 22px 提到 38–48px（按宽度），压紧上方留白，观测窗口标注再压暗，与效果图里走势占格高约三分之一一致。
  - **未采纳的一项**：效果图页脚把来源做成一排芯片，实测拆分后我们的真实来源名（`FRED H.10（DTWEXBGS自动更新）` 等）每个只剩 7 个字符可见，比原来的整行文本更难读——芯片形态只在效果图那种 8 字符占位厂商名下成立。已回退，改为再放宽来源格宽度。
  - **修复本轮引入的一处回归**：第一次提交给行情带读数钉了 `line-height: 1.16`，行盒被固定后不再随字体度量伸缩；本地是文泉驿、CI 是 Noto CJK，在 CI 上字形盒更高就出现 `scrollHeight > clientHeight`，踩中浏览器探针的 `overviewCardLegibility`（1280px 失败）。已移除该行高覆盖并在原处留下注释说明不可再钉死；走势图改为靠上方腾出的留白长高，`min-height` 回到原值，空间紧张时不再顶破卡片。另经对照测量确认：首屏本来就会滚动（main 基线同为 928px / 视口 925px），滚动条不是本轮引入的。
  - 离线校验全绿（80 项）；360 / 768 / 1280 / 1440 / 1672 五档实测无横向溢出、无页面报错、无文本截断；CI 浏览器回归在 360 / 768 / 1280 三档通过。**已部署**（2026-08-27，经所有者确认后合并 `main`，`Finance Terminal Quality` run 33109809534 全绿；GitHub Pages 由 `main` 直接提供服务）。

- 2026-08-27，**首页与参考效果图第二轮逐块对齐**。上线后所有者仍反馈「差距很大」，本轮把上一轮没抓住的四个形态差异补上，仍然只改呈现，不动行情、来源、时效、代理披露与降级契约。
  - **行情带改成参考稿的弧带**：之前是平直矩形条，效果图是一条两端收窄、上下缘为椭圆弧的发光带。改法是把水平圆角半径取满宽的一半、纵向上缘 42px 下缘 20px，让上下边框各自连成一整段椭圆弧；格子背景改透明由弧形透出，六格不再靠纵向位移假装曲线（参考稿里读数是平齐的）。同时解开分区通用样式的 `overflow: hidden` 与矩形描边——那两条会把弧带裁回矩形。
  - **图标换成图形徽章**：证据条三项改为圆形描边内嵌 SVG（地球 / 盾牌 / 水滴）；行情带六格改为实心彩色徽章，按品类取色（红 / 蓝 / 灰 / 金 / 白 / 橙），替掉此前的细描边等宽字形。
  - **地球**：边缘光改为方向性渐变，左上缘最亮、绕到右下熄灭，与参考稿一致；极光帘幕加强并偏洋红；城市连线弓高加深并提到地球画布之上——此前连线被画布盖住，只有球体外的一小段可见，现在是横跨球面的长弧。
  - **工作流预览补密度**：收益率曲线补节点与「当前 / 历史对比」图例；事件时间线补高 / 中 / 低影响等级色签；资产影响强度条由单条渐变改为三段式。预览仍是 `aria-hidden` 装饰底图，不含任何数值。
  - **页脚状态条**：每组标签前补参考稿的小图标块，来源与状态两格再放宽，减少省略号；来源名保持站内真实管道（FRED / EIA / CoinGecko / TradingView），未改成效果图上的示意机构名。
  - 离线校验全绿（80 项）；360 / 768 / 1280 / 1440 / 1672 五档实测无横向溢出、无页面报错、无文本截断；CI 浏览器回归在 360 / 768 / 1280 三档通过。**已部署**（2026-08-27，经所有者确认后合并 `main`，`Finance Terminal Quality` run 33081429029 全绿；GitHub Pages 由 `main` 直接提供服务）。

- 2026-08-27，**首页与参考效果图逐块对齐（参考首页精修第三版）**。所有者反馈“实际落地图与效果图差别太大”，本次只改呈现，不动任何行情、来源、时效与降级契约。
  - **地球回到正圆**：撤掉把画布横向拉伸 1.22 倍的变换，按参考稿把球心摆在总览区 45% / 63% 处，直径约等于参考稿的比例；同时撤掉画布上三条装饰性卫星椭圆与背景网格线，纬经线、陆地点阵与边缘辉光整体压暗，读起来是夜地球而不是线框球。
  - **市场连线改为屏幕空间绘制**：新增 `finance-terminal-orbit-links.mjs`，按四个城市标记的实测位置算出弧线并随尺寸重算，连线永远落在标记点上，不会因为地球自转而漂走。连线与图注一致，仍是时区连接示意，不表示资金流向。
  - **极光帘幕**：地球上缘补上参考稿里的紫青色极光（`screen` 混合），窄屏与减少动画偏好下不受影响。
  - **四个状态表盘重画**：环形从放射状齿纹改为参考稿的 3/4 弧环（用边框拼弧，不依赖 `mask`），环内换成内联 SVG 图标（地球 / 盾牌 / 波形 / 数据库），环径与读数字号按参考稿收窄。
  - **证据条改为判语在前**：三块证据由“标签在上、读数在下”改为参考稿的“判语在上、证据标签在下”，并按内容自适应宽度，`中性 · NEUTRAL` 这类读数不再被截断；≤620px 单独排一行。
  - **行情带按参考稿重排**：读数与涨跌同排，走势图吃满每格剩余高度并补上面积填充；观测窗口标注（`RECENT OBSERVATIONS · n / 8`）改为浮在右上角的小字，既保留口径又不再压掉走势；图标只占名称那一行，不再与数字叠字；免费代理格的组件区同步撑满。来源、数据日、代理说明与状态在“核心资产”视图与页脚状态条里保持完整。
  - **三个工作流入口预览补密度**：品类行情加表头与分隔、跨资产趋势补 K 线与量能底图和时间轴、收益率曲线补 3M–30Y 期限轴、流动性监测补三格指标位、事件时间线与资产影响预览补表头与行结构。预览为 `aria-hidden` 装饰底图，不含任何数值。
  - **顶栏与页脚**：主导航与模式切换补上参考稿的 `·` 与 `/` 分隔，模式切换收成一颗胶囊；页脚状态条放宽来源与状态两格、字号提到可读区间。
  - 新增受预算约束的 `terminal-reference-home-v3.css`，离线校验补上新样式层与连线模块的契约（实测坐标、随尺寸重算、禁用 `innerHTML`）。360 / 768 / 1280 / 1440 / 1672 五档实测无横向溢出、无页面报错、无文本截断；CI 浏览器回归在 360 / 768 / 1280 三档全绿，免费代理组件 2/2 挂载、0 次官方链接降级。**已部署**（2026-08-27，经所有者确认后合并 `main`，`Finance Terminal Quality` run 33078391505 全绿；GitHub Pages 由 `main` 直接提供服务）。

- 2026-08-27，**金融终端默认首页落地为「极光全息指挥台」**（所有者确认参考方案后执行）。
  - **首屏层级收敛**：桌面默认页只保留“今日首先看什么”的规则摘要、全息地球、四项已校验状态、六资产弧形行情带，以及「专业终端 / 宏观研究 / 实时事件」三个工作流入口；原资产、品类、风险、研究、资讯和运行证据视图均保留，不删除既有功能。
  - **不使用示意行情**：首页HUD只镜像既有风险雷达、宏观状态、OFR金融压力和核心资产卡的已校验读数；核心覆盖同时计入免费组件运行时不可用状态，缺数据时显示空态，不另算或补造指标。全息地球仍明确是时区连接示意，不表示资金流向。
  - **两种信息密度**：顶部新增「标准模式 / 专业模式」切换；标准模式回到全球总览，专业模式进入六大品类行情板。主导航重组为「全球总览 / 专业终端 / 宏观 / 实时事件」，其余研究与工程入口继续保留在完整导航和终端侧栏。
  - **移动端独立编排**：≤1040px 改为纵向任务流，四项HUD变为2列或1列，三个工作流入口在手机上单列展示；所有新按钮保持44px触控目标，支持键盘焦点、强制颜色和减少动画。
  - **时效表述与可见口径同步修正**：页眉和底部状态栏明确使用「实时 / 准实时 / 日更 · 逐项标示」，不再把混合频率统称实时；同时把旧的「8项资产 / 4项免费代理」可见残留修正为当前真实的「6项资产 / 2项免费代理」，根首页中英文入口也同步。
  - 新增受预算约束的 `terminal-aurora-home.css` 与 `finance-terminal-aurora-home.mjs`，同步扩展离线校验和浏览器回归契约；本地离线数据、加载器、品类行情与地缘风险契约均通过。**未部署**。

- 2026-08-26，**品类行情再扩容到170项，并给每个标的一个真实网址的完整行情页**（所有者按 TradingEconomics 的覆盖面提出）。
  - **扩容**：跨资产清单 56→98 项，品类行情合计 128→170 项。商品加取暖油、RBOB汽油、可可、橙汁、燕麦、糙米、豆油、豆粕、活牛、饲牛、瘦肉猪、木材（16→28）；指数加荷兰、比利时、瑞典、奥地利、土耳其、波兰、泰国、马来西亚、菲律宾、智利、阿根廷、罗素2000、恒生科技与VIX（24→38）；外汇加港元、墨西哥比索、南非兰特、土耳其里拉、泰铢、瑞典克朗、挪威克朗与两组欧系交叉盘（13→22）；债券加TIP、AGG、LQD、HYG、MUB、EMB、BWX（15→22）。五个指数在数据源上取不到，改用对应国家ETF回退并逐条写明「美元计价、含汇率影响」；波动率指数VIX单独注明不是可交易标的。
  - **点开是一个真实网页，不是弹层**：逐行改成 `<a href="quote.html?kind=…&symbol=…">`，可新标签页打开、可复制分享、可被浏览器前进后退。详情页按类别只读对应管道的快照。
  - **走势区间到 5年 / 10年 / 25年 / 全部**：新增 `scripts/market_history_long.py` 与三份月线长历史（跨资产、公司榜、美债曲线）。美债各期限可回看到 1962 年，公司榜最早的到 1962 年，跨资产多数到 1984 年或该标的上市月。加密目前只有一年日线，对应的长区间标签显示为不可用而不是拿短区间顶替。
  - **走势图重画**：渐变面积、发光折线、按标签长度自适应的纵轴刻度与网格、起止日期轴、区间极值标注，以及鼠标或方向键都能读数的十字游标；下方给区间变化、最高、最低、观测点数与起止日期，收益率类按基点、其余按百分比。
  - **一个诚实性修正**：数据源对超长区间会自行降采样（部分标的早年只有季度末观测）。若按数组下标等距作图，稀疏时段会被拉成和密集时段一样宽——形状是假的。现在图按真实时间轴摆点，长区间按「月数」而不是「点数」裁剪，并额外取一次近十年月线把近端补稠密。
  - 上游明确 `unavailable` 且无价格的标的不再进入列表（那种行只有一串「—」）。
  - **与 main 上的极光首页改版合并**：品类行情落在「专业模式」，两条线互不打扰；合并时保留 main 更新的数据快照，随后在 main 上重跑跨资产、公司榜与宏观雷达三条管道，扩容后的 98 项标的（品类行情 170 项）与三份月线长历史一并生效，98 项当轮全部取到真实行情、无 `unavailable`。
  - **已部署**（2026-08-28，经所有者确认后合并 `main`；`Asset Tracker` #113、`Companies Tracker` #112、`Macro Radar` #112 与 `Finance Terminal Quality` #161 全绿；GitHub Pages 由 `main` 直接提供服务）。

- 2026-08-26，**地缘风险定价改为独立分区、移到原ETF代理那一格，并重画成半圆表盘**（所有者指定位置与风格）。
  - **位置**：桌面一屏总览第三行右侧那一格（即标普500/纳斯达克100 ETF代理卡撤下后腾出的位置）现在放地缘风险定价；核心资产整块上移到第二行、与品类行情并排。核心资产是移位不是隐藏——免费ETF代理组件必须有真实布局才挂得上，代理挂载证据依赖这一点。品类行情相应从13列收到9列，六大类标签、迷你走势与全部列都保留。
  - **不再寄居在「市场聚合指标」里**：它现在是自己的分区 `#geo-risk-section`，长页与窄屏里排在三张官方信号卡之后；那三张卡「口径不同、不合成为新的总分」的角标也随之改回 `NO SYNTHETIC DATA`。
  - **重画为半圆表盘**：五档弧段（当前档亮起并带辉光）、弧上游标标出读数、中心是分数与量纲；读数用游标而不是指针，指针会横穿分数反而更难读。四条轴的读数条按各自所处档位着色，与表盘同一套配色。
  - **总览里也是展开的**：表盘 + 四条轴的名称、精简原值（如「布油 -9.97%」「金减股 +12.09pp」「FSI -2.67 · 第15百分位」）与分数条都在；逐轴完整原值、映射口径、来源、数据日与更新时间在「市场状态」视图里展开。
  - 360/430/768/1280 四档实测通过，浏览器探针的 `geoRiskModel`（四条轴、五档、只亮一档）继续生效。
  - **已部署**（2026-08-26，经所有者确认后合并 `main`，`Finance Terminal Quality` #121 全绿）。合并前已并入 `main` 上的13次日更数据并重跑全部校验：换数据后模型读数由 37 变为 32/100「偏低」。

- 2026-08-26，**新增「地缘风险定价」四轴模型**，落在首页「市场聚合指标」分区（原标普500/纳指ETF代理撤下后腾出的位置）。
  - **名字按它真正能读的东西起**：它读的是市场为地缘风险付出的价格，不统计也不解读地缘政治事件本身。事件型的地缘风险指数（如 Caldara–Iacoviello GPR）只提供 `.xls/.dta`，站内也没有足够密度的新闻语料，做不到就不冒充——页面与方法说明都写明了这一点。
  - **四条轴，全部来自站内已在日更的公开管道**：能源溢价（布伦特近一月涨幅，按 ±15% 线性映射；同时显示 WTI 作旁证）、避险需求（黄金减标普500近一月涨幅差，按 ±15 个百分点线性映射）、波动率制度（宏观雷达波动率信号，风险方向取 100 减信号分，与首屏风险雷达同一取向）、金融压力（OFR 金融压力指数在该文件自身观测窗口内的百分位）。四轴等权合成 0–100，落到低/偏低/中性/偏高/高五档。
  - **逐轴都摊开**：原值、映射口径、来源、数据日与上游更新时间逐条显示；映射用的区间写在卡面上，读者可以自己复算。**任一轴缺失即不给等级**，只写明缺哪一条，不用其余轴的平均顶替；上游过期会在整体上标注出来。
  - **与同分区那三张官方信号卡刻意分开**：三张卡「口径不同、不合成为新的总分」这条规矩没有变，模型单独占一块、单独描边，也不参与那三张卡的 ACTIVE/STALE 计数；总览角标同步从 `NO SYNTHETIC DATA` 改为 `3 VERIFIED SIGNALS · 1 PRICED MODEL`。
  - **不新增任何外部依赖与请求**：跨资产快照这一份资源键在品类行情与市场研究两组里已经在取，加载器按键去重，总览里请求数不变（探针实测仍是 5 → 9 → 14、重复 0）。
  - **可视化**：五档等级梯（当前档高亮并带辉光）、四条轴各一根按自身等级着色的读数条；总览一屏里只留名称、等级与等级梯，逐轴明细在「市场状态」视图里完整展示。
  - 新增离线契约 `scripts/validate_finance_terminal_geo_risk.mjs`（映射端点、百分位、等级边界、权重合计、缺轴/窗口过短/上游失败一律不出等级、过期传导、原值与分数同时给出）并接入 `Finance Terminal Quality`；浏览器探针新增 `geoRiskModel` 检查（四条轴、五档梯、只亮一档；缺轴时改判「不可用」态）。360/768/1280 三档全绿。**已部署**（2026-08-26）。

- 2026-08-25，**品类行情：逐行迷你走势 + 六品类强调色 + 涨跌分布脉冲条**（所有者要求「不要太单调」）。
  - **每行一根60交易日走势**：数据全部取站内已经发布的历史序列（跨资产 `history.json`、公司榜 `history.json`、加密 `crypto.json`、FRED 收益率曲线 `curve.json`），不新增任何外部来源与请求路径；同一品类最多触发一次历史文件读取，读取本身沿用抽屉那份带缓存的实现。
  - **走势的颜色按它自己的窗口首尾算**，与当日涨跌分开：白银可以「今日涨、60日跌」，两者互不改写。站内还没有序列的标的显示「无序列」并给出原因，不画任何推断曲线，也不借用相邻标的。
  - **六个品类各有一条强调色**（商品琥珀、指数青、股票紫、外汇碧、加密橙、债券蓝）：标签圆点、选中态、列表描边与顶部渐变都跟着当前品类走。颜色只做品类标识；涨跌仍由箭头、正负号与文字承担，不靠颜色单独编码。
  - **脉冲条**：标题右侧一条上涨/持平/下跌占比条，配 `▲x ▼y ▬z` 计数，只统计当前显示（含搜索与自选筛选后）的行；债券的方向词按该品类自己的口径读作「上行/下行」。计数与该品类摘要同源，离线契约里逐条对齐。
  - **窄屏让位**：≤620px 隐去迷你走势与口径列，优先保证名称、价格、涨跌不被挤扁；两者在走势抽屉里都有完整版本。总览一屏里走势列压到62px、脉冲条压到54×4px。
  - 360/430/768/1280 四档实测：探针 `categoryBoard` 增加「每行都有走势位、脉冲条已渲染」两项断言；离线契约新增迷你走势取窗、方向判定与分布计数三组纯函数测试。**已部署**（2026-08-26）。

### 修改

- 2026-08-25，**一屏总览重排 + 撤下标普500与纳斯达克100两张ETF代理卡**（所有者决定）。
  - **主行让给品类行情**：原来放8张核心资产卡（含4张ETF代理）的那一行改成「品类行情」，六大类在总览里直接可读、可切换；行宽变满后把口径/市值那一列也放了回来，行数由4行增到5行。
  - **核心资产退到第三行**与「市场强弱与领袖」「市场聚合指标」并排。它没有被整块藏起来是有原因的：免费嵌入组件必须有真实布局才挂得上，代理挂载证据依赖这一点——藏起来会让总览里的代理运行时校验直接判失败，且卡片会退化成官方链接。退到窄格后卡片改两列纵向铺开，并在总览里隐去★与「详情」入口（「资产」视图里原样保留，元素仍在DOM中）。
  - **底部恢复「市场强弱与领袖」**：全球股指当日表现地图与三张研究卡回到一屏总览；「稳定V1运行证据」仍是独立视图。窄屏两块的明细继续默认折叠。
  - **撤下 `sp500`(SPY) 与 `nasdaq100`(QQQ)**：核心资产由8项收敛为6项——2项免费嵌入代理（DIA、GLD）+ 4项站内官方管道（DGS10、DTWEXBGS、RWTC、BTC/USD）。同步收窄的不只是 `data.json`：免费代理许可契约 `market-source-readiness.json`、许可门禁、发布门禁、浏览器代理证据工具与回归探针全部改为按已登记的代理清单计数，不再写死4项；页面横幅、许可提示、方法说明与朗读文案同步改口。
  - **纳斯达克改由综合指数进入品类行情**：所有者当日确认把纳斯达克综合 `^IXIC` 加入跨资产清单的指数类（清单 55→56，指数类 23→24）。它不是 `QQQ` 原本跟踪的纳斯达克100（`NDX`），成分与数值都不同，`data.json` 的 `note`、文档与校验断言都写明这一点，页面不用前者顶替后者；`NDX` 与 `^DJI` 仍不进入清单（道指继续由 `DIA` 免费组件展示）。标普500的点位同样看指数类（`^GSPC`，不是ETF代理）。
  - **顺手改掉的写死值**：行情带原本要求「恰好8项资产」否则整条显示不可用，改为只要求有资产可播（条数由 data.json 契约校验负责）；探针里的卡片数、代理数、可读性元素数与汇总输出改为按常量或实测值推导。
  - 360/768/1280 三档浏览器回归全部通过；离线校验、许可门禁、发布门禁、代理证据契约全绿。
  - **已部署**（2026-08-25，经所有者确认后合并 `main`）：线上核心资产已是6项，跨资产清单已按56项发布（56/56全部为当轮真实行情，含纳斯达克综合 `^IXIC` 26,102.18、标普500 `^GSPC` 7,666.49），品类行情六大类合计128项标的。
  - **合并后被CI抓到一处遗漏**：代理运行趋势脚本把「3档视口 × 4项代理 = 12个宿主观测」写死在契约里，代理收敛为两项后当轮的6个观测被判覆盖无效，`Finance Terminal Quality` #119 卡在最后的「Build bounded proxy runtime history」（前22步含真实Chrome三档回归全过）。按符号搜 SPY/QQQ 搜不到该文件——那里只有数字。已改为当轮按「视口数 × 已登记代理项数」复算、历史周期读各自当时声明的观测总数（旧周期不被现在的项数改写，也不因缺少 `proxyCountPerViewport` 字段被判无效），夹具同步按项数推导；#120 全绿。

### 新增

- 2026-08-25，**品类行情板扩容四步全部完成**：股票与加密补上日线、跨资产清单扩到55项、行情板加入搜索与自选、页面按「一屏看行情」重排。
  - **股票与加密可以画走势了**：新增 `scripts/market_history.py`，跨资产、公司榜、加密快照三条管道共用同一份滚动历史规则；公司榜为市值前40支各存一年日线（`apps/companies/history.json`），资产榜复用同一次 CoinGecko 取数输出前20币的行情与日线（`apps/asset-ranking/crypto.json`）。两份都是主快照之后的附加步骤，失败只跳过自己。
  - **清单28→55**：商品加天然气、铂、钯、小麦、玉米、大豆、咖啡、白糖、棉花；外汇加加元、瑞郎、纽元、韩元、印度卢比、新加坡元、巴西雷亚尔；指数加台湾加权、加拿大S&P/TSX、瑞士SMI、西班牙IBEX35、意大利富时MIB、墨西哥IPC、印尼雅加达综合、以色列TA-125；债券加美债长/中/短三段ETF代理。**不新增美国基准指数点位**，`SPX`/`NDX`/`DJIA` 与 ICE `DXY` 继续按既有许可决定处理，并加断言防止日后被顺手加进来。
  - **条数不再写死**：跨资产卡与运行证据卡的条数跟随健康文件与快照实际发布的条数；健康契约在扩容期同时接受已登记的28与55，覆盖率仍必须按文件自己声明的期望值复算。
  - **搜索与自选**：按中文名/英文名/代码字面搜索，搜索时每个品类标签显示「命中数/总数」；每行行首★/☆，自选置顶且与核心资产卡共用同一份本地清单，任一处切换两边同步重画。
  - **页面瘦身**：窄屏把「市场强弱与领袖」「稳定V1运行证据」的明细收进默认折叠的 `<details>`（标题与摘要仍可见）；桌面一屏总览把这两块移出栅格（各自仍是独立视图，左栏与分区导航可进），第三行改成「市场聚合指标 + 品类行情」，行情板在总览里直接可读、可切换品类。
  - **回归探针同步**：折叠壳默认状态按断点断言；没有渲染出来的栅格不参与列数断言（改由桌面分区切换逐个核对）；分区跳转的「掠过不加载」不变式改盯页面中段的品类行情板。360/768/1280 三档浏览器回归全部通过。
  - 免费数据源调研与扩容执行情况记录在 `docs/MARKET_DATA_SOURCES.md` 第12节。
  - **已部署**（2026-08-25，经所有者确认后合并 `main`）：跨资产已按新清单发布55项（55/55全部为当轮真实行情，0项回退、0项缺失），公司榜已发布40支的260个交易日日线，资产榜已发布20个加密行情。合并后手动触发的 `Finance Terminal Quality` 全绿，含 GitHub 机房里真实 Chrome 的 360/768/1280 三档回归。上线后行情板六大类合计127项标的。
  - 首轮实测发现 **CoinGecko 免费档对日线请求限速比预想更紧**：按2秒间隔连发时第5个请求起就被拒，20个币里只取到4个的日线，文件按契约如实标为 `partial`。已把日线间隔放宽到10秒、失败再等30秒重试一次、连续4次被拒即本轮提前收工；已取到的照常写入，其余沿用上次序列，历史在若干轮里补齐，不靠一次跑满、也不补造点位。改动上线后重跑一轮：20个币的行情全部就位，日线覆盖 **4 → 17**，文件状态回到 `ok`，剩余3个在后续轮次继续累积。

### 新增

- 2026-08-25，金融终端新增**「品类行情」分区**：商品、指数、股票、外汇、加密、债券六大类共 **105 项标的**，此前首页只有 8 张核心资产卡。
  - **零新增数据源、零新增密钥、零浏览器直连行情API**：六类全部读取站内已在日更的公开管道——商品/指数/外汇/中国国债来自跨资产管道（Yahoo Finance），商品另含宏观雷达的 EIA 库欣WTI现货，股票来自全球公司榜，加密来自全球资产榜（CoinGecko），美债11个期限来自宏观雷达的 FRED 官方收益率曲线。分区加载器只多请求一份 `macro-radar/curve.json`，其余资源与研究、运行证据分区共用，重复网络请求仍为 0。
  - **列表可折叠**：每类默认只画首屏行数（股票 8 行、其余 6 行），其余收在「展开全部 N 项」后面；只渲染当前品类，切换标签才重画，不一次插入上百行。
  - **逐标的走势抽屉**：点任意一行打开来源与口径（最新价、涨跌与其口径、数据日、更新时间、频率、来源、状态、代理原标的、计价货币）加站内历史走势，支持 1个月/3个月/6个月/1年区间，显示区间高低、覆盖日期、观测点数与区间变化。抽屉复用资产详情抽屉的外壳与折线映射，焦点、Esc、遮罩关闭只有一份实现。
  - **口径不混用**：股票为当日价格变动、加密为过去24小时、债券为相对上一观测的基点（bp）变化，逐行标注；债券行的主数值是年化收益率而非债券价格；基点变化由曲线历史最后两个观测按 `(今日 − 上一观测) × 100` 复算，离线断言核对。
  - **没有序列就说没有**：股票与加密目前只有站内最新报价，抽屉如实说明站内暂无历史序列并给出官方来源，不用相邻标的或推断值顶替；共享日期轴上缺观测的交易日直接跳过，不做前向填充。
  - **按许可决定排除 DXY**：跨资产管道里的 ICE 美元指数 `DX-Y.NYB` 不进入本页，外汇类只展示美联储广义美元指数 `DTWEXBGS`，与既有免费优先许可结论一致；跨资产强弱卡只比较各标的自身收益率，不受影响。
  - **失败只降级一类**：任一上游管线读取失败时只让对应品类为空并在分区内写明是哪条管线失败，其余品类照常显示，整体状态显示为部分可用；上游标记过期的标的逐行显示「过期」并计入该类汇总。
  - **可访问性与响应式**：标签为 `role="tab"` 并支持左右方向键，行本身是按钮且触控目标不低于 44px，涨跌同时用箭头、正负号与文字表达（颜色只是第三重编码），360/768/1280 三档实测无横向溢出；非美元计价的股票在价格旁显示货币代码。
  - **桌面单屏不受影响**：单屏指挥中心的总览栅格保持原样，品类行情在桌面是自己的一个视图（左栏 `03 品类`、分区导航「品类行情」），1040px 及以下仍按长页顺序排在核心资产之后。
  - 新增 `finance-terminal-board-data.mjs`（数据层，17KB 预算）与 `finance-terminal-board-view.mjs`（视图，15KB 预算），两者均为按需导入、不在首屏预加载；`app.js` 159,361 字节、常规加载合计 200,401 字节，仍低于 220KB 与 230KB 预算。分区加载器预算 15,000 → 15,600、回归探针 19,000 → 21,000，均在文件内注明原因。
  - 新增 `scripts/validate_finance_terminal_board.mjs` 离线契约：六类顺序与折叠阈值、逐行来源/数据日/频率/口径、只收上市公司真实行情、基点变化可复算、单管线失败隔离、过期传递、区间裁剪不前向填充；已接入 `validate_finance_terminal.py` 与只读质量工作流。浏览器回归探针新增品类行情板检查，并把键盘与标签语义改为逐「标签组」校验（页面现有跨资产周期与品类行情板两组）。
  - **已部署**（2026-08-25，经所有者确认后合并 `main`）。


### 修改

- 2026-08-23，**风险雷达六个轴改为各读一项真实制度信号**，不再由单一分数加权重组。
  - **数据一直都在，只是终端没读**：宏观管线本就抓 VIX、VIX3M、HYG/LQD 等序列并算出 8 个制度信号（波动率来自 VIX 水平与期限结构、信用来自高收益债 OAS 275bp、流动性来自净流动性 5.79T 与 SOFR−IORB）。此前终端只读聚合后的一个分数，把分项全丢了——雷达才不得不用那一个数字重组出六个轴，名义上叫波动率、信用、流动性，实际并无对应测量。`signals` 此前在 `app.js` 里一次都没出现。
  - **映射**（信号口径「越高越宽松」，风险 = 100 − 分数）：实际利率 ← `realrate`、期限溢价 ← `term`、美元汇率 ← `usd`、波动率 ← `volatility`、信用利差 ← `credit`、流动性 ← `liquidity`。轴名同步更名，去掉没有对应测量的「通胀风险」。
  - **形状变了，这才是重点**：综合分 4.7 → 4.6 几乎不变，但六轴离散度由约 1.9 增至 **7.6**。真实数据显示实际利率（8.9）与流动性（6.9）才是压力点，波动率（1.3）与信用（2.6）宽松；旧版把六个轴全挤在 3.5–5.4，等于把风险抹平了。
  - **缺任一信号即整体空态**，六边形少一个顶点无法成形，不用其他轴的值顶替。六个信号各缺一次的隔离情形均有断言覆盖。
  - 抽屉重写为逐轴列出来源信号、原始分数、状态与其依据原文（如「VIX 水平与期限结构」「高收益债 OAS 275bp」），并注明分数是近两年窗口分位、不是对后市的预测。抽屉与雷达共用同一份信号键，任一方改动映射即被断言抓住。
  - **零新增数据源、零新增请求、零新增密钥。**
  - `finance-terminal-risk-radar.mjs` 预算由 3,000 上调至 3,400：原值是按旧版那行加权重组设定的，六个具名信号的查表不可压缩地更大。该模块不计入 230KB 常规加载合计，增量约占首屏 JS 的 0.2%。

### 修改

- 2026-08-24，**app.js 瘦身**：把只服务单个分区的适配代码搬进按需加载模块，首屏 JS 合计由 227,145 降到 **198,214** 字节（预算 230,000），余量从 2,855 变为 **31,786**。
  - **搬走了什么**：事件资讯分区的经济日历与财经新闻适配（12.4KB → `finance-terminal-information-data.mjs`）；运行证据分区的宏观逐源健康校验、稳定V1资格快照与七条管线运行卡片（19.1KB → `finance-terminal-operations-data.mjs`）。这两块只有各自的分区用得上，此前每个访客在首屏都要下载。
  - **纯搬运**：19 个函数体全部**逐字照搬**，已用脚本比对确认与 main 上的文本完全一致；只把 app.js 里仍要共用的助手与常量改为注入（沿用视图模块既有的依赖注入写法）。
  - **实测行为不变**：360 / 768 / 1280 三档回归全部 `pass`，且与 main 上跑同一份探针的结果逐字相同——事件资讯 2 张卡、运行证据 4 张卡、稳定V1 证据面板 4 个、风险 3 张、研究 3 张，四个分区模块状态均为 ready。
  - **离线测试改为走真实接线**：适配函数不再从 app.js 直接取，而是像运行时一样 `createInformationData(adapter.sectionDataHelpers())` 之后再调用——测试因此覆盖了注入这一层。
  - **契约同步**：新增两条断言锁定「适配函数不得留在首屏脚本里」，两个新模块各有独立字节预算（15KB / 23KB）。

- 2026-08-23，收益率曲线在「市场状态」页标题栏**补一个入口**。此前只放在总览的遥测卡里——那是当时恰好有空位，不是因为那里合理。收益率曲线属于市场状态，访客会本能地去那一页找它。两个入口共用同一个懒加载模块，那一页原本只有三张聚合指标卡（宏观状态、恐慌贪婪、OFR压力），加上曲线才凑成一组完整的市场状态面板。

### 修复

- 2026-08-24，修复**单屏总览把分区内容横向裁在边界外**——「OFR金融压力」那张卡整个不见了。
  - **现象**：总览里风险区那一格宽 354px，里面的内容宽 490px。三张聚合指标卡各 163px，第三张（OFR金融压力）从 846px 起、到 1009px 止，而分区在 872px 处 `overflow: hidden`——**只剩 26px 露在外面**，看上去就像被右边的「稳定V1运行证据」面板盖住了。运行证据面板自己同样被裁掉 201px。
  - **根因**：`#main-content > section` 已经写了 `min-width: 0`，但分区**里面那一层**（`.risk-grid`、`.board-heading`、`.pipeline-command`）是分区的网格项，`min-width` 默认 `auto`，不肯缩到 min-content 以下，于是整块溢出、被 `overflow: hidden` 齐刷刷切掉。没有报错，没有横向滚动条，内容只是从边界起消失。
  - **修法**：给分区的直接子元素补 `min-width: 0`。三张卡改为各 117px（1512px 下）/ 98px（1280px 下），全部完整显示。
  - **顺带修好收益率曲线入口**：遥测面板里是三个可见块（状态、口径、收益率曲线）却只排了两行，曲线那块落进隐式轨道分不到高度，「1M 至 30Y 曲线 →」按钮 24px 里有 6px 被裁在面板外。第三行改按内容取高，并把卡片与口径格的内边距各收紧 3px，三块才装得下。
  - **补上本该拦住它的断言**：CI 浏览器校验里新增一条——总览分区内**纵向可见**的元素，右边界不得越过分区。撤掉修复后它立刻报出 8 处越界（`div.board-heading` 越界 195px、`div.risk-grid` 195px…）。
  - **预算**：`terminal-command-center.css` 由 20,000 上调到 20,700 字节。

- 2026-08-23，修复**分区导航一跳，整页数据被一并拉起**——延迟加载在窄屏上等于没做。
  - **现象**：360px 下点导航栏的「资讯」或「运行证据」，浏览器会平滑滚动上万像素过去。沿途每一个分区都在视野里掠过约两百毫秒，`IntersectionObserver` 照单全收，于是风险区、研究区的请求全部被拉起。原本按需加载的 13 个数据源，一次跳转就取回大半。
  - **为什么一直没被发现**：CI 里那三段「分区请求复用边界」断言，快照都取在「该分区刚就绪」那一刻，而滚动动画此时还没走完——它们是抢在动画前面取样才通过的。取样早一点就绿、晚一点就红，`风险区在 operations 阶段被拉起` 因此反复偶发。这次是在未改动的 `origin/main` 上本地复现出与 CI 完全一致的报错，才确认它与雷达那个改动无关。
  - **修法**：视野触发改为「停留才算数」——分区进入视野后需持续停留 600 毫秒才加载，中途划走即取消。掠过不再触发，导航点击仍然立即加载目标分区，正常滚动阅读的体验不变。
  - **顺带合并了一处各行其是的实现**：`app.js` 里为首屏风险雷达单独写的那个观察器已删除，改为把雷达面板登记成 risk 分区的第二个触发元素（一个分区现在可以有多个触发元素）。两处触发逻辑合成一处，`app.js` 反而小了 817 字节，常规加载合计从 228,499 降到 227,145 字节。
  - **补上正反两条断言**：滚动停稳后再看一次，跳去别的分区不得把风险区拉起来；反过来，停在首屏雷达前，风险数据必须自己取回来（窄屏下 `#risk-section` 在四千像素之下，雷达曾因此长期空着）。离线契约测试另加一条：掠过不加载、停留才加载、同一分区的第二个触发元素不重复加载。
  - **预算**：`finance-terminal-loader.mjs` 由 14,000 上调到 15,000 字节。

- 2026-08-23，修复 **DTWEXBGS 每周有一半时间被误报为「数据已过期」**。页面顶部那条黄色告警不是抓取故障，而是阈值用错了假设。
  - **诊断**：站内抓取一直成功（`status: healthy`、连续失败 0 次）；FRED 确实返回了数据，只是最新观测停在 08-14。翻 git 历史发现关键证据——**一周前的同一天（08-16）滞后天数一模一样是 5 个工作日**，且 08-16 到 08-21 之间它一次性从 08-07 跳到 08-14，正好一周的数据。
  - **根因**：DTWEXBGS 的观测是日度的，但美联储 H.10 **按周成批发布**，每周一次性补齐上一周。滞后天数每周在 1 到 5 个工作日之间循环。原阈值 `DTWEXBGS_MAX_BUSINESS_DAYS = 3` 是按日频假设定的，导致它从每周三四起一直误报到周日。
  - **修法**：阈值改为 8 个工作日——正常周期内不再误报，而一旦某周批次真的缺失，两三天内仍会触发（实测：数据停在 08-14 时，08-28 周五滞后 10 天正确报警）。频率标签由「日频 · 自动更新」改为「日度观测 · 每周成批发布」，过期说明也改为按周批次口径表述。
  - **顺带修好两处脆弱测试**：一条断言 `success.status === "stale"` 此前之所以恒真，正是靠这个误报——它并非在测汇总逻辑，而是在断言当天真实数据碰巧是什么；已改为锁不变式（有任一卡过期则页面过期，否则正常）。另一条把阈值 `3` 写死在测试里，与源码是两处独立数字；已改为读取被测代码导出的同一个常量。刻意构造的过期夹具同步推到 10 个工作日前，确保它仍落在过期区间内。

### 新增

- 2026-08-24，新增**资产相关性矩阵**（「市场强弱与领袖」页标题栏 → 抽屉）。用站内已发布的 `asset-tracker/history.json` 现算，零新增数据源、零新增请求、零新增密钥；只有点开才下载这段代码与那份历史。
  - **用收益率，不用价位**：格子里是两个标的**日对数收益率**的皮尔逊相关系数。价位序列本身带趋势，两条各自上涨的价位曲线相关系数天然接近 1，那个数字不描述「一起动」。
  - **不跨休市日拼接**：只用两个标的都有相邻两日报价的交易日；缺报价处不前向填充，也不把隔着休市日的两点之差当作当日收益率。重叠交易日少于 60 天的格子以斜纹留空，不给出不稳定的系数。
  - **交易日历错位的标的整列剔除**：站内历史里若某标的的观测落在周六周日，说明其日线时间戳未按交易所当地日期归档，与其余标的错位；这种样本算出的系数是错的（实测欧元兑美元与美元指数应当接近 −0.95，错位样本只给出 −0.39）。抽屉里逐个点名被剔除的标的与其周末观测数，并说明原因，而不是悄悄少显示几行。本轮 28 个标的中 8 个因此未纳入，矩阵为 20×20、190 组配对、最少重叠 121 天。
  - **明写不是预测**：相关系数是所选窗口内的历史统计，不表示其中一个标的的变动导致了另一个。
  - 抽屉复用资产详情抽屉外壳，13,323/15,000 字节按需加载，不计入 230KB 常规加载合计。新增 21 条离线断言覆盖收益率口径、日历错位判定、样本不足留空与常数序列。

- 2026-08-23，新增**美债完整收益率曲线**（1M / 3M / 6M / 1Y / 2Y / 3Y / 5Y / 7Y / 10Y / 20Y / 30Y）。此前站内只有 DGS10 一个点，现在是整条期限结构。数据来自 FRED 官方序列，滚动保留约一年历史，落盘为 `apps/macro-radar/curve.json`。
  - **不插值、不顶替**：某期限当日无观测就在图上断开折线并留空，绝不用相邻期限的值补齐；缺档会在抽屉里逐个点名。孤立的单点不连线——两侧都缺时无法确定走向，宁可只画一个点。
  - **非当日观测单独标注**：读数存在但不是最新数据日的期限，图上以空心点显示，并列出其实际数据日。
  - **利差只在两端同日都有观测时计算**：跟踪 10Y-2Y 与 10Y-3M，倒挂是该口径下的事实陈述，抽屉里明写「不构成对后市的预测」。
  - **零新增密钥**：复用既有的 `FRED_API_KEY`；DGS10 本轮已在别处取过，直接命中进程内缓存。
  - 曲线抽屉 7,964 字节按需加载，复用资产详情抽屉的外壳（焦点、Esc、点遮罩关闭），不计入常规加载预算。入口在「市场状态」区，首屏即绑定。
  - 新增 18 条离线断言：缺档必须断线不得插值跨过、两侧各剩一点不画线、对数横轴短端不被压缩、零跨度不产生 `NaN`、少于三个可用读数不给形态结论、缺读数档位不计入形态判断。生成端另有四组情形测试：全部正常、缺两个期限、某档滞后一日、FRED 全失败保留上次文件。

### 修复

- 2026-08-23，修复**点阵世界地图完全没有渲染**。上一次改动删除了旧的 `ANCHORS` 常量，却漏掉了辉光那段仍在引用它——`paint()` 一进入就抛 `ReferenceError`，而它跑在 `image.onload` 回调里，异常被吞掉：页面不报任何错，只是静默回落到被拉伸的降级贴图，看起来"有点糊"而已，三档浏览器检查依然全绿。改动前 95,053 个着色像素，改动后 0 个。
  - 辉光改用边界框中心定位后**索性去掉**：它会把 −0.3% 这种寻常波动放大成一片燃烧的橙色，与刚更名的「当日表现」定位不符。涨跌由点本身的颜色如实表达。
  - **补上本该拦住它的断言**：区域明细已渲染出行时，点阵画布必须真的画出内容（就绪且着色像素 ≥1000）。该断言放在 CI 专用的浏览器校验脚本里，而不是有 19KB 预算的回归探针内——探针只在 `?regression=1` 时加载，且已无余量。

### 修改

- 2026-08-23，点阵世界地图**清晰度修正**。画布背衬此前固定 900×430，却被 CSS 拉成约 3:1 显示：点阵被竖向压扁、纵横比失真，且只用到 1600×800 遮罩的一小部分。改为按画布实际显示尺寸乘设备像素比设定背衬，并由同一尺寸推导采样格数，使点阵在任何宽高比下都是方格。
  - 实测：1512px 下纵横比由 `2.09 vs 1.36`（失真）变为完全一致；设备像素比 2 时背衬像素数为显示的 4 倍。768px 与 360px 滚动到地图后同样一致。
  - 因 canvas 在拿到 `risk-map-canvas-ready` 前是 `display:none`、量不到尺寸，绘制流程改为先加该类再测量，绘制失败则撤回并回落到降级贴图。

### 修复

- 2026-08-23，跨资产收益率页把日频数据标成「**实时行情**」，已改为「日频行情」。该页数据 `frequency: daily`、每天更新一次，原措辞既与数据本身矛盾，也和同一句里的「每日自动更新」自相冲突，违反项目「不得把日频、延迟或静态数据标成实时」的规则。同页「实时数据上线后可用」一并改为「该周期暂无足够数据」——它描述的本就是周期数据不足，与实时无关。

- 2026-08-23，修复**世界地图给没有代表指数的地区上色**。此前用「单一锚点 + 46 度圆半径」就近取色，欧洲的圆一直盖到尼日利亚与埃及，印度的圆盖到沙特：站内七个区域代表指数里没有任何非洲、中东或俄罗斯标的，地图却替这些地区断言了市场状态。改为按各区域实际经纬度边界框判定，框外一律按无数据着色，重叠处取面积更小者。新增 22 条地理断言：11 个必须覆盖的城市（含乌鲁木齐、里斯本、新德里等边界情形）与 11 个必须留白的城市（拉各斯、开罗、利雅得、莫斯科、首尔、雅加达、曼谷等）。

- 2026-08-23，热力图入口此前指向跨资产收益率页，但那里没有「区域」这个维度、默认显示年初至今，与地图上的当日涨跌对不上（同一个日经，地图 −0.30%，点进去 +31.1%）。改为指向站内「强弱与领袖」页——真正的七区明细本就在那里完整显示。

### 修改

- 2026-08-23，热力图更名为**「全球股指当日表现」**（原「全球风险热力图」）。它显示的是七个区域代表指数的当日涨跌，不是风险度量；原标题与其副标题 `REGIONAL EQUITY PRESSURE PROXY` 本身就自相矛盾。副标题同步改为 `REGIONAL EQUITY DAILY RETURN · 7 代表指数`，图例由 `LOW / HIGH PRESSURE` 改为「当日上涨 / 当日下跌」，导航 aria-label 同步。

### 新增

- 2026-08-23，风险雷达新增**「六轴如何算出」构成抽屉**，把六个轴的来历原样摊开。此前图上六个数字凭空出现，唯一的交代是底部一行小字。
  - **摊开的是一个此前不显眼的事实**：这六个轴不是六项独立测量。站内只有三项已校验信号（宏观状态、恐慌与贪婪、OFR金融压力），六个轴由它们按固定权重线性重组而来。抽屉逐条列出每个轴的算式，例如「利率风险 · 宏观状态 100%」「通胀风险 · 宏观状态 70% + OFR金融压力 30%」。
  - **明写名称与数据的落差**：抽屉注明「利率风险」「通胀风险」等名称描述的是该权重组合意在近似的风险类型，不代表站内存在对应的利率、通胀或信用专项数据——「利率风险」就是宏观状态本身，「通胀风险」不含任何通胀输入。
  - **输入不全时不推算**：三项输入缺任一项时，雷达保持空态，抽屉也只说明输入不可用，不显示由不完整输入算出的轴值。
  - **加了漂移守卫**：抽屉的权重表与 `deriveRiskRadar` 是两份独立实现，校验脚本在 5 组输入下逐轴比对二者输出，任何一方改动而另一方未同步，测试立即失败。
  - 抽屉外壳（焦点归还、Esc 关闭、点遮罩关闭）从资产详情抽屉中抽出共用，避免两份可访问性逻辑各自走样；资产抽屉行为经实测未变。
  - 模块 4,709 字节，按需加载不计入常规加载预算。入口在首屏即绑定，不等风险区渲染完——窄屏下风险区可能一直没加载，若把绑定挂在渲染回调里，按钮在那些尺寸下会是点了没反应的死键。

### 修复

- 2026-08-23，修复**窄屏下风险雷达长时间停在 `LOADING`**。
  - **根因**：雷达画在首屏所在的 `#overview-section` 里，它的数据却属于 `risk` 延迟分区，而分区的加载触发点观察的是 `#risk-section`。桌面一屏装得下两者，同时可见所以从未暴露；窄屏下 `#risk-section` 位于四千多像素之下，雷达便一直空着，**滚到雷达跟前也不会加载**——必须一路滚到那个不相干的分区才行。
  - **修法**：为雷达面板本身补一个加载触发点。窄屏下滚到雷达即加载；768px 下雷达落在预载范围内，页面打开即有数据。桌面行为不变。
  - 实测对照（干净 `main` vs 修复后，滚到雷达面板时）：360px `LOADING`／0 张卡 → `MODERATE`／3 张卡；768px 同样由空态变为有数据。
  - 更正：此前把该现象记为「窄屏下根本不加载」，措辞不准确——它会加载，只是要滚到数千像素之下的另一个分区才触发。

### 新增

- 2026-08-23，总览各面板补上**通向对应数据页的入口**，并把「这里能点」做得看得见。此前站内已有 29 个独立应用页，其中 8 个正好是这些面板的数据源头，但总览上几乎没有连过去。
  - 新增四个入口：全球风险热力图 →「跨资产追踪」；市场洞察与动态 →「市场状态」页；稳定V1运行证据 →「V1运行证据」页；全球市场总览（地球）→「数据说明」页。
  - **修好了一个更隐蔽的问题**：聚合指标三张卡（宏观状态、恐慌与贪婪、OFR金融压力）**本来就能点进各自的数据页**，但承载入口的 `.risk-footer` 在桌面总览被整块隐藏，入口跟着一起消失——站主看着截图都没发现它们可点。卡内尚有约 100px 空余，现在只把入口露出来，来源与时间仍留给完整视图。
  - **补上缺失的 `hashchange` 监听**：视图切换此前只监听 `popstate`，而点击 hash 链接只触发 `hashchange`。少了这一条，导航栏以外的入口点下去地址变了、视图却不动。
  - 全站可点入口统一为带边框的胶囊样式，悬停高亮、具备可见焦点环；触控目标沿用既有口径（桌面 ≥24px，窄屏 ≥44px）。1512/768/360 三档共 16 个入口全部可见且达标，无横向溢出。
  - 顺带删掉一条已被新规则覆盖的重复声明，腾出的字节用于本次改动，未上调任何预算。

### 修复

- 2026-08-23，修复**总览卡片的主数字被裁掉大半截**。DGS10、美元指数、WTI、比特币四张卡最显眼的数字，装它的盒子只有 **9px 高，字号却是 22px**，加上 `overflow:hidden`，页面上只看得到下半截；来源署名页脚同样被裁。
  - **根因**：总览要把整页压进一屏，每张卡只分到 125px，卡里却塞了八个区块（标题、主数字、涨跌、走势小图、口径说明、更新链健康、数据日、来源页脚）。弹性挤压时，主数字与页脚是会被压缩的那两个，于是最该看清的东西最先糊掉。
  - **修法**：总览里让主数字与来源页脚保持自身高度，并把走势小图、口径说明、更新链健康、数据日移出总览——后四者原本就已被压成 0 高度、根本看不见，且在「核心资产」页与详情抽屉里都有完整版本。四项元素仍留在 DOM 中，逐源健康与观测走势的既有契约不受影响。
  - **加了断言防复发**：新增 `overviewCardLegibility` 回归契约，锁定总览的 4 个主数字与 8 个来源页脚都不得被裁。已验证撤掉修复后该契约立即失败。360/768/1280 三档均通过。

### 修改

- 2026-08-23，门禁校验**不再在第一处失败就停下**。此前 `validate` 作业的 17 个校验步骤逐个串行，任一失败后面全部不跑；每日资格作业则是一整块 `bash -e` 脚本，同样停在第一条。结果是缺陷会一层挡一层——2026-08-23 修掉的三处缺陷正是如此，其中浏览器检查已很久没有真正跑到，两处线上回归因此长期未被发现。
  - `validate` 作业：17 个校验步骤加 `if: ${{ !cancelled() }}`，彼此不再阻断。
  - 每日资格作业：改为逐项记录成败，最后按累计状态退出；每个失败项以 `::error::` 单独标注，日志按 `::group::` 分组。
  - **门禁强度不变**：只要有任一检查失败，作业仍然失败。改变的只是「一次运行能看见多少问题」。
  - 依赖前序产物的步骤（如构建代理运行时历史）仍保持默认的 `success()` 条件，不会在前面失败后跑出误导性的二次错误。

### 修复

- 2026-08-23，修复经济日历**每逢周六即自判过期**的缺陷，该缺陷会让终端在每个周末把一份完好的周历标为「过期」，并使门禁校验在周末必红。
  - **根因**：`weekOf` 此前取「恰好有事件的那几天」的首尾。周六周日没有经济数据发布，区间因此总在周五结束；而周六跑出的文件 `asOf` 是周六，落在自己声明的周范围之外，下游 `outsideWeek` 判定随即触发。
  - **生成端**：`weekOf` 改为表示这批事件**所属的完整一周**（周日~周六，与 Forex Factory 周历同一口径），而不是碰巧有事件的那几天。这让该字段的含义与其名字一致。
  - **消费端**：`adaptEconomicCalendar` 的周范围判定同样按整周对齐，使既有文件无需等下一次日更即可恢复正常。
  - **没有放宽陈旧判定**：36 小时未更新仍判过期；文件进入下一周仍判过期。两条都已加断言锁定，与「周六不得误判」一并构成三条回归测试——去掉修复后测试即失败。

- 2026-08-23，修复**事件资讯分区的两块更新链健康面板长期显示 UNKNOWN**。辅助来源健康适配层此前只在「市场状态」分区加载时安装，而「事件资讯」分区同样要渲染这两块面板，于是它先渲染、面板显示「辅助来源健康适配层尚未加载」。改为两个分区都等适配层装好再解析视图。该缺陷由 2026-08-22 的 `app.js` 瘦身引入：离线测试显式安装了适配层，浏览器路径没有，因此未被测出。

- 2026-08-23，修复**城市时钟的 `<time>` 元素混入时区缩写**导致其内容不可被程序解析（如 `23:32 EDT`）。时区缩写移入相邻元素，`<time>` 只保留机器可读的 `HH:MM`，页面上的可见效果不变（仍显示 `23:32 EDT`）。

### 新增

- 2026-08-23，核心资产卡片新增**数据详情抽屉**：每张卡右上角「详情」按钮打开一个只读面板，集中展示该标的的完整来源口径、站内可得的官方长序列、同币种高影响事件与标题命中的最新条目。这是终端第一次让访客能追问一张卡「这个数字从哪来、以前长什么样」。
  - **不生成任何新的行情事实**：抽屉只重排已有数据，不做插值、不用相邻标的顶替、不推断缺失值。
  - **没有序列时如实说明为什么没有**，分两种情况分别措辞：四张 TradingView 免费组件代理卡写明「按其使用条款不得抓取、保存或再分发组件行情，因此站内没有可绘制的历史序列」，并给出官方来源链接；站内序列尚未攒够点位的标的写明「日更任务累积后此处会显示完整曲线，在此之前不显示推断值」。
  - **相关性措辞刻意保守**：同币种高影响事件按事件自身的币种与影响级别筛选，标注「不代表其与本标的存在因果关系」；新闻仅按标题字面命中，标注「未做语义判断，命中不等于与本标的相关」，并显示实际命中的关键词，让访客能自行判断匹配是否成立。
  - **按需加载，抽屉自身的样式也按需注入**：不打开详情的访客既不下载模块，也不为这段 CSS 付出首屏成本；模块 11,704 字节，不计入常规加载预算。但**触发按钮的样式留在首屏 CSS**——验证时发现放进懒加载模块会让按钮在模块到达前只有 42px 高，正好是访客要点它的那一刻低于 44px 触控目标。
  - 可访问性：`role="dialog"` + `aria-modal`，打开即把焦点移到关闭键，Esc 与点击遮罩均可关闭，关闭后焦点归还原按钮；折线图带 `role="img"` 与写明起止日期和观测点数的 `aria-label`。360/768/1280 三档均无横向溢出，触发按钮 36×44、关闭键 44×44。
  - 折线几何为纯函数 `seriesPath`，与标题命中判定 `matchedKeyword` 一并纳入离线断言（12 条）：不足两点不画线、非有限值整点剔除、全平序列不产生 `NaN` 坐标、极值贴合内边距、未命中返回 `null`。

### 修改

- 2026-08-22，`app.js` 瘦身：把仅服务「市场状态」延迟分区的辅助来源健康适配逻辑（`adaptSupportingSourceHealth` 与其独占的 `supportingComponentPresent`、`SUPPORTING_HEALTH_SPECS`）移入按需加载的 `finance-terminal-health-adapters.mjs`。
  - **真实减负而非挪账**：该分区本就延迟加载，不打开「市场状态」的访客不再下载这段代码。常规加载合计 **229,893 → 221,108 字节**，余量由 107 涨到 8,892。
  - **函数体逐字未改**：共享辅助（`isNumber`／`hoursSince`／`sameStringArray`／`sourceHealthPercent`）与历史状态常量由调用方注入，因此迁移前后逻辑完全相同。已用 main 上的旧实现与新实现做对拍：4 个数据集 × 5 个时间点共 20 组输入（覆盖正常、过期与抛错三类路径）输出**逐字节一致**。
  - 新增显式安装点 `installSupportingHealthAdapter`，浏览器分区加载与离线测试走同一条路；**未安装时明确返回「适配层尚未加载」，不臆造健康状态**。
  - 校验脚本同步改为从新模块构造适配层，原有 10 条辅助健康断言全部保留。

- 2026-08-22，宏观三条官方序列开始沉淀**滚动一年长历史** `apps/macro-radar/series.json`：DGS10、DTWEXBGS（FRED）与 RWTC（EIA）各保留最近 260 个交易日。
  - **零额外请求**：`fred_api` 本就为 2 年分位打分抓取 520 个观测点并进程内缓存，此前展示时被截断到 8 个即丢弃，现直接复用同一份缓存；RWTC 把单次 EIA 请求的 `length` 由 8 调大，仍是同一次请求，结果在轮内缓存供两处使用。
  - **不动受契约约束的 8 点窗口**：`data.json` 中三项官方行情的「最近 8 个观测点」是上线门禁明文核对的口径，本次新增的是独立文件，两者互不影响；校验中已加断言锁定这一点。
  - 本轮取不到的序列沿用上次，不为新日期补造点位；三条全失败时返回空，保留上次文件不覆盖；非法日期与非正值一律剔除。

- 2026-08-22，核心资产新增**自选清单**：每张卡片右上角 ☆ 收藏开关，自选项在**资产网格与顶部行情条同步前置**，市场区标题出现「只看自选 N」筛选入口（无自选时自动隐藏）。这是终端第一次记住访客的偏好。
  - **只存本机**：仅保存标的代码到 `localStorage`，不上传、不随任何请求外发；模块与筛选入口提示均写明此点。
  - **存储不可用时如实降级**：隐私模式或站点数据被拦时读写会抛异常，此时降级为本次会话内存态，功能仍可用但不跨会话保留，提示改为「浏览器未允许本地存储，自选仅在本次会话有效」——不假装已保存。
  - **输入清洗**：只接受形似标的代码的短字符串（长度与字符集受限），拒绝任意内容写入本地存储；自选上限 40 条并去重。
  - **不改任何数值口径**：前置只影响呈现顺序。跨资产「领涨/领跌」排行榜**刻意不做重排**——在排行榜里把自选置顶会让「领涨」名不副实。
  - 状态由 ★/☆ 符号本身表达，颜色不作为唯一信号；收藏按钮 360/768/1280 三档触控目标均为 44px，具备可见焦点环。

- 2026-08-22，跨资产管道开始沉淀**滚动一年日线历史** `apps/asset-tracker/history.json`：28 个标的的收盘价序列，滚动保留最近 260 个交易日。这是后续卡片详情图、相关性矩阵与阈值提醒的共同前提。
  - **零新增网络请求、零新数据源**：`fetch_series` 本就为计算涨跌幅抓取整年日线，此前算完即丢弃；现改为一并落盘，与 `data.json` 同一次取数、同一来源与时点。
  - 采用与 `macro-radar/history.json` 一致的紧凑格式：共享日期轴 + 逐标的数值数组，独立文件可懒加载，不拖慢首屏。
  - **不做前向填充**：共享日期轴上该标的当日无报价即为 `null`，如实反映各市场交易日历差异（如日股休市而美股开市）。
  - 本轮取数失败的标的**沿用上次序列**，既不丢历史也不为新日期补造点位；全部标的均无有效序列时返回空，调用方保留上次文件不覆盖；`NaN` 与缺失值一律剔除。
  - 仓库膨胀已实测而非估算：单份文件 68.6 KB，经 git 增量压缩后**每日提交净增约 2.1 KB，一年约 0.5 MB**；滚动截断使文件大小恒定，不随时间增长。

### 修改

- 2026-08-22，`market_workflow_governance.stage_owned` 改为只暂存磁盘已存在或 git 已跟踪的授权路径。此前对已登记但尚未生成的产物（如首次运行前的 `history.json`）会因 pathspec 不匹配导致整个 `git add` 失败；已跟踪但已删除的路径仍保留，删除动作照常可被暂存。

- 2026-08-22，全息地球四个城市牌从纯装饰变为**交易时段盘**：新增 `finance-terminal-sessions.mjs`（独立3.5KB预算），按各所常规交易时段与周末纯日历推导状态——`常规时段` / `午间休市` / `待开盘` / `已收盘` / `周末休市`，并给出距下一次状态变化的时长。含上海与东京的午间休市：上半场剩余时间指向「午休」而非「收盘」。
  - **刻意不计入交易所假日**。假日表需逐年维护且会临时调整，一旦过期就会在圣诞节这类日子谎报「开盘中」；因此对外措辞统一为「常规时段」（字面永远为真），并在地球说明行明确披露「按各所常规交易时段与周末计算，未计入交易所假日」。
  - 不读取任何行情数据、不新增外部请求；时区解析失败时返回「时段未知」，不猜测开盘。
  - 状态除颜色外同时用符号（●／◐／○）与文字区分，颜色不作为唯一信号。

- 2026-08-22，金融终端新增点阵世界地图模块 `finance-terminal-worldmap.mjs`（受5KB独立预算约束）：用站内 `earth-water.jpg` 海陆遮罩在画布上生成大陆点阵，按七组区域代表指数的**既有当日回报**着色（跌幅越深由琥珀转橙红、上涨为冷青、无代表指数为暗蓝并单独可辨），压力区叠加辉光扩散。地图只把已有回报值映射为颜色，不新增数据源、不产生新的行情事实；画布不可用时保留原SVG色块层作为降级。未合并、未部署。

### 修复

- 2026-08-22，修复聚合指标读数被圆环截断：`宏观状态`与`恐慌与贪婪`的 `54 / 100`、`52 / 100` 因超出圆环宽度被省略号截成 `54 / …`。量纲后缀改为独立成行，读数完整可见；状态徽标移到圆环下方，卡片内容纵向居中以消除底部约100px空档。OFR金融压力为原始指数（非0–100分），改用**未填充的中性轨道环**替代原星芒底纹：与百分制表盘视觉统一，同时如实表明其没有可归一化的量纲，不伪装成进度条。
- 2026-08-22，修复桌面单屏标题换行溢出：`市场聚合指标`等看板标题在窄栏内逐字换行，撑破28像素标题行并与下方网格重叠；标题改为不换行，右侧状态计数在空间不足时先省略。
- 2026-08-22，修复地球画布就绪后旧SVG连线弧仍然叠加显示的问题，避免两套轨迹并存。
- 2026-08-22，修正风险雷达评分标签错字（`风险合评分` → `风险综合评分`）。

### 修改

- 2026-08-22，地球动画由夜景纹理贴图改为全息线框球：以同源海陆遮罩采样生成点阵大陆，配倾斜正交投影的经纬网、限边辉光与前后分层轨道环（后半段先于球体绘制、前半段在其后，形成真实环绕关系）。保留 `textureCoordinate` 纹理换算契约、自转、`prefers-reduced-motion` 与页面隐藏时暂停；遮罩不可用时回退 `earth-night.jpg`，再失败则回退原SVG球。模块仍在8KB预算内。
- 2026-08-22，城市时钟补充时区缩写：按时区选择locale以取得正确缩写（伦敦 `BST` 需 `en-GB`、纽约 `EDT` 需 `en-US`），其余时区显示准确的GMT偏移，不硬编码会随夏令时改变的缩写。
- 2026-08-22，风险雷达评分区按参考构图重排：标签、读数与 `/10` 居左，新增右下角风险判语（低/中等/高三档，各含一句处置提示），判语与既有 `MODERATE` 状态徽标同源于同一档位，不另造评分口径。

### 文档

- 2026-08-22，`finance_terminal_v1_qualification.yml` 的推送触发路径补齐四个 `terminal-*.css` 样式表与地球、雷达、视觉、世界地图四个模块。此前纯CSS改动不触发资格工作流，导致分支上停留在过期的失败结论；补齐后样式修复也会重新验证。
- 2026-08-22，`validate_finance_terminal.py` 新增点阵世界地图模块的5KB预算与契约断言（同源遮罩、既有回报着色、禁止 `innerHTML`、保留SVG降级），与风险雷达模块采用同一治理方式。

### 状态

- 本批只改呈现层与其校验：未改动任何行情数值、来源、数据日、更新时间、过期判定、失败回退或稳定V1资格记录，未新增外部请求与密钥。
- 参考样板中的付费实时数据源、Bloomberg / CME / Refinitiv 数据源行、管线吞吐量与延迟指标、以及 `Ooglex GPT-Market` 模型署名**未予复制**：本站无这些来源与能力，照搬会构成 `AGENTS.md` 禁止的伪造数据与来源。样板把上海标注为 `GST`（海湾时间UTC+4）亦为错误，本站按实际显示UTC+8。
- 本地检查：`validate_finance_terminal.py`、`validate_finance_terminal_visuals.mjs`、`validate_finance_terminal_loader.mjs`、`validate_finance_terminal_market_licenses.py`、`validate_market_data_quality.py`、三份来源健康校验、工作流治理与门禁契约共14项全部通过；360/768/1280像素分别复算 `noHorizontalOverflow`（文档不横向滚动且20张卡片均在边界内）与卡片数量契约，三档均通过。
- 未执行项：`validate_finance_terminal_browser.mjs` 完整回归在本沙箱无法跑完——全页截图（360px下页面高16911像素）超过Node内置WebSocket解压上限导致连接中断，属环境限制而非页面缺陷；该脚本在GitHub Actions中正常运行，已用等价探针逐档复算其溢出与卡片断言。
- 未合并 `main`、未部署。

- 2026-08-22，金融终端按定稿样板完成高保真视觉精修：全息地球新增深海明暗、大气辉光、夜景大陆、城市灯光、经纬网与尊重减少动画偏好的独立动态；桌面首屏新增由宏观状态、恐慌贪婪和OFR压力三项既有信号归一化映射的六维风险雷达，并明确标注为结构可视化代理；三项聚合指标HUD与全球风险热力图同步强化环形刻度、蓝色海岸辉光和琥珀压力色阶。所有新增脚本和样式采用独立性能预算，不改行情、来源、更新时间、过期、失败回退或稳定V1资格。未合并、未部署。
- 2026-08-21，金融终端新增与科幻样板一致的桌面单屏指挥中心：16列桌面网格同屏编排全息地球、8项核心资产、全球风险热力图、3项风险HUD、事件资讯与四管线`STABLE V1 5/7`证据；顶部行情带、左侧快捷栏和分区导航同步收紧。导航点击可切换至原有完整详情，1040px及以下继续顺序阅读，不复制行情、不生成AI事实、不改来源、更新时间、过期、失败回退或已有资格记录。未合并、未部署。
- 2026-08-21，金融终端新增真实数据驱动的科幻分析层：区域热力图以Yahoo Finance七组代表性股票指数的当日价格回报计算市场压力代理并逐区披露标的、代理、数据日和更新时间；三张市场状态卡改为圆形HUD但不混合不同指标口径；四管线HUD从既有资格快照动态读取最小连续周期，保留`5/7`及证据过期状态，不清空、不补造、不重复累计。未合并、未部署。
- 2026-08-21，金融终端新增机构级科幻视觉外壳：左侧快捷导航、核心资产状态行情带和全息地球时区面板均使用本地HTML/CSS/SVG实现；纽约、伦敦、上海与东京时间由浏览器实时换算，连接轨迹明确标为示意且不表示资金流，原有价格、来源、更新时间、过期和回退语义保持不变。未合并、未部署。
- 2026-08-21，金融终端分区加载新增机器可复算证据：记录首屏关键请求集合、绘制/调度顺序、分区启动顺序、逐请求状态、重复网络请求计数及四分区状态转换；离线测试与三档Chrome回归共同阻止提前加载、重复请求和静默失败，证据不含行情值。未合并、未部署。
- 2026-08-14，金融终端补齐CoinGecko公开产品页面契约：新增产品使用条款与隐私政策，覆盖CoinGecko所有权、禁止转售/再分发和绕过限制、责任排除、加密资产波动、静态页面数据处理、TradingView组件与外部链接；页脚提供常驻入口，“Powered by CoinGecko”使用独立11px归属样式。验证器检查内容、移动端可读性、外链隔离和零新增追踪脚本。未合并、未部署。
- 2026-08-13，CNN、OFR、经济日历与财经资讯辅助资格工作流接入同一免费代理7周期趋势和运维评估：四源刷新后的最终页面证据、趋势、Markdown摘要和截图统一保留；辅助资格自身不进入趋势历史，也不扩大核心管道资格门槛。未合并、未部署。
- 2026-08-13，稳定V1四管道资格工作流接入免费代理7周期趋势和运维评估：最终检出最新开发分支、完成三档Chrome后生成JSON/Markdown、写入Actions摘要并随代理证据保留14天；资格运行自身不进入历史源，也不改变核心3/7与7/7计数。未合并、未部署。
- 2026-08-13，免费代理7周期趋势新增可复算运维评估：连续两周期三档脚本加载且12个宿主观测全挂载才为`HEALTHY`；单周期、部分挂载或首次恢复为`WATCH`；连续两周期脚本失败或12/12全回退为`WARN`；收集不完整为`UNKNOWN`。评估不读取行情、不替代核心3/7与7/7资格门槛。未合并、未部署。
- 2026-08-13，金融终端质量工作流新增免费代理7周期运行趋势Artifact：从目标开发分支最近14天证据按21:00 UTC边界去重，同周期仅取最新、最多保留7周期，并汇总脚本失败分类、宿主挂载、官方链接回退与关联诊断；历史API失败诚实标记`partial`并只保留本次真实证据，不读取行情、不虚增核心资格周期。未合并、未部署。
- 2026-08-13，SPY、QQQ、DIA与GLD三档浏览器证据新增精确TradingView官方回退URL与实际可见性；契约要求组件不可用时链接可见、宿主挂载时链接隐藏，且不主动请求外链、不读取组件报价。未合并、未部署。
- 2026-08-13，三档Chrome运行证据将TradingView白名单脚本传输失败归类为DNS、TLS、连接、超时、阻断或其他受控类型，并在汇总与Markdown中复算；证据拒绝原始浏览器错误文本、响应正文及任意网络日志，不读取组件行情。未合并、未部署。
- 2026-08-13，四项TradingView免费代理卡新增分层失败说明：加载超时、组件注册失败、浏览器验证不可用与宿主挂载异常分别显示，所有状态继续保留官方标的链接且不回填缓存值、演示值或组件报价。未合并、未部署。
- 2026-08-13，三档Chrome运行证据新增TradingView白名单脚本传输状态：记录固定脚本是否请求、HTTP成功/失败、缓存、浏览器阻断或响应等待，只保留受控枚举、状态码和缓存布尔值；不保存响应正文、任意日志、其他网络请求或组件行情。未合并、未部署。
- 2026-08-13，代理运行证据新增传输到宿主的可复算关联诊断，并生成同Artifact的人类可读Markdown摘要；运维人员可区分脚本传输失败、组件注册超时、浏览器运行时限制、部分挂载与宿主验证失败，诊断仍不宣称报价渲染、新鲜度或开市状态。未合并、未部署。
- 2026-08-13，金融终端三档Chrome检查新增`finance-terminal-browser-evidence.json`：分别记录360/768/1280像素下SPY、QQQ、DIA与GLD的宿主挂载或官方链接回退状态，只允许代理代码、状态和受控原因，拒绝价格字段及伪造报价渲染/新鲜度结论。质量、稳定V1与辅助资格工作流均校验并以14天Artifact保留该证据，不写入Git、不虚增日更周期，未合并、未部署。
- 2026-08-13，SPY、QQQ、DIA与GLD四项免费代理从“全局组件已注册”提升为逐卡宿主挂载验证：每个`tv-mini-chart`必须已连接、已定义并具有非空布局才显示，否则该卡保留TradingView官方标的链接；挂载证据仍明确不代表报价已渲染、行情新鲜或市场开市。离线契约、异常夹具和三档浏览器探针同步覆盖，未合并、未部署。
- 2026-08-13，SPY、QQQ、DIA与GLD四项TradingView免费代理新增统一组件运行时状态：页面显示加载中、已注册或未加载，8秒超时后只保留官方标的链接，脚本晚到注册时自动恢复；“已注册”明确不代表报价已渲染、行情新鲜或市场开市。机器契约、离线单测和三档浏览器探针同步覆盖，不新增依赖、密钥、本地行情或收费源。未合并、未部署。
- 2026-08-13，项目所有者将三大指数与黄金改为免费优先方案：金融终端以TradingView官方Web Component展示`SPY`、`QQQ`、`DIA`与`GLD`，逐卡明确披露其分别代理`SPX`、`NDX`、`DJIA`与`LBMA Gold Price PM`且不是同一原标的；不需要行情API密钥，不抓取、导出或保存组件原始行情，组件失败时只显示官方标的链接。四项本地演示值已全部移除，精确原标的询价方案暂停并保留为未来付费升级预案；同日更早的“精确原标的锁定/询价准备”记录为被本决定取代的历史过程。未合并、未部署。
- 2026-08-13，金融终端首屏新增精确原标的授权进度条：读取同源机器契约并显示`INQUIRY READY / INQUIRY SENT / QUOTE RECEIVED / LICENSED`，当前准确显示SPX、NDX、DJIA与LBMA Gold Price PM询价材料已准备但未提交、未授权，四项继续`DEMO`。契约加载或复算失败时显示`LICENSE UNKNOWN`且不放开真实数据；三档浏览器回归同步覆盖。未合并、未部署。
- 2026-08-13，稳定V1开发分支新增跨日观察标记与契约：按21:00 UTC边界识别新周期，同周期重复执行不改文件，标记推送只触发四管道资格验收；成功周期仍由远端工作流记录复算，标记不能虚增1/7进度，且硬声明不合并、不发布、不部署。未合并、未部署。
- 2026-08-13，三大股指与黄金新增机器可读公开展示授权契约：逐项登记精确原标的、官方条款依据、许可权利人、当前阻塞状态、允许生产动作及SPY/QQQ/DIA/GLD显式代理候选；上线门禁拒绝无授权编号的伪批准、阻塞状态发布和ETF静默冒充原指数。行情数值与生产数据未修改，未合并、未部署。
- 2026-08-13，全球资产榜卡片接入`asset_ranking.yml`稳定V1周期证据，四张生产管道卡现均展示各自1/7进度、最近周期与运行链接；三档浏览器回归新增四证据面板、进度语义、链接安全与移动端触控检查。未合并、未部署。
- 2026-08-13，全球公司榜卡片新增`companies.yml`稳定V1周期证据，500家公司数据健康、慢频估值覆盖与1/7远端资格分层展示；证据异常不会改写公司榜生产快照。未合并、未部署。
- 2026-08-13，跨资产强弱卡片接入同一稳定V1静态证据契约，独立显示`asset_tracker.yml`连续成功1/7、最近周期和可复核运行链接；证据加载或校验失败只降级本卡证据，不覆盖28项资产数据与健康状态。未合并、未部署。
- 2026-08-13，宏观官方序列卡片新增稳定V1远端证据：显示连续成功周期、最近周期、7周期进度及对应Actions运行；资格流程从只读门禁报告生成受结构校验的静态快照，只精确提交该文件，同周期结论不变时跳过提交，不调用行情API、不合并、不部署。
- 2026-08-12，Google News五板块与同份快照中的Yahoo市场行情纳入开发分支辅助来源资格验收；四条辅助任务先全部启动再独立等待，一条失败不会取消其他真实刷新。未改写生产数据，未部署。
- 2026-08-12，Forex Factory经济日历纳入开发分支辅助来源资格验收；与CNN、OFR先同时启动再独立等待，失败只阻断本次资格结论且不取消其他来源。复用既有真实取数与快照回退，未改写生产数据，未部署。
- 2026-08-12，跨资产管道新增ETF、期货与替代指数的结构化代理契约，记录目标代码、实际工具、币种、价格/总回报口径和误差说明；金融终端与资产详情页显式显示`PROXY`，无效或静默替代会被数据和前端校验拒绝。本批仅提交开发分支，未部署。
- 2026-08-12，BTC/USD卡片新增资产榜逐源`UPDATE HEALTH`：CoinGecko、Yahoo降级或历史回退必须分别对齐来源计数、同批成功时间与健康状态；价格可读但更新链证据缺失、过期或错配时不再显示为健康，上线门禁同步阻断不一致证据。本批仅提交开发分支，未部署。
- 2026-08-12，EIA RWTC成功刷新会把实际访问路径（API v2或官方公开历史页）同时写入行情与逐源健康证据；WTI卡片的`UPDATE HEALTH`直接显示访问路径，旧快照缺失时明确为“待记录”，未登记路径会被校验和上线门禁拒绝。本批仅提交开发分支，未部署。
- 2026-08-12，金融终端BTC/USD卡片复用全球资产榜中唯一的Bitcoin/BTC逐条行情：CoinGecko日度快照显示“Powered by CoinGecko”和24小时涨跌；Yahoo `BTC-USD`降级显示`PARTIAL`及“较前收盘”；两源失败才保留同标的旧快照并标记`STALE`。
- BTC/USD加入稳定V1只读门禁，校验来源、唯一性、正数价格、涨跌范围、逐条时点、36小时时效和快照对齐；估值、未知、不可用或未来记录不能冒充真实行情。核心真实数据覆盖由3/8提升至4/8，剩余4项演示资产仍逐卡标注。
- 2026-08-12，新增CNN Fear & Greed与OFR两条辅助来源的开发分支资格验收：两源先同时启动、单源失败不取消另一源，并以触发前运行ID基线排除旧记录；完成后从最新分支验证四源健康、失败保留、路径治理和360/768/1280像素页面。
- 辅助资格报告、四源健康诊断和响应式截图仅作为7至14天Actions Artifact保留；流程不会扩大或替代核心四管道的Beta 3周期与稳定V1 7周期门槛。
- 2026-08-09，新增稳定V1四管道开发分支资格验收：先并行启动宏观雷达、跨资产与公司榜，公司榜成功后再运行全球资产榜；单源失败不会取消其他独立管道。
- 资格运行完成后会重新读取开发分支最新提交，执行完整数据、来源健康、失败回退、工作流治理、Beta/稳定V1门禁和360/768/1280像素真实浏览器检查，并将报告、健康诊断与截图保留为7至14天Artifact。

### 安全

- 辅助来源资格流程硬限制为`agent/finance-terminal-*`开发分支，只拥有Actions调度和仓库内容只读权限；不写入`main`、不创建发布、不部署网站。
- 资格验收硬限制为`agent/finance-terminal-*`开发分支并拒绝默认生产分支；令牌只进入GitHub API请求头，报告不保存凭据，流程不合并`main`、不创建发布、不部署网站。

### 修复

- 2026-08-22，金融终端按定稿参考图完成第二轮细节校正：桌面右栏由四列收窄为三列，将地球、核心资产与三项独立风险指标恢复到更接近样板的首屏比例；地球由SVG呼吸效果升级为站内夜间地球纹理的真实经度自转，增加球面投影、明暗边缘、经纬网、大气辉光与本地纹理失败回退，并在页面隐藏或系统减少动画时暂停。六维风险雷达新增五层刻度和逐轴真实映射值，全球风险热力图新增本地世界夜景纹理与七区域数据驱动光斑，市场洞察仅以三项已验证信号生成明确标注的规则摘要。行情、来源、时点、频率、过期、失败回退及稳定V1 `5/7`证据均未改动；未合并、未部署。
- 2026-08-21，桌面顶部行情带将四项TradingView代理的重复长句收敛为“组件报价”，在不读取、不保存组件价格的前提下让SPY、QQQ、DIA、GLD与四项站内真实资产在样板宽屏中完整露出；资产卡与完整详情继续保留代理、时效及失败回退说明。未合并、未部署。
- 2026-08-21，桌面单屏指挥中心首轮真实Chrome回归修复隐藏研究标签页的键盘探针误聚焦，并恢复右栏详情入口至少24px桌面目标高度；质量截图默认高度由旧长页1400px改为与最终样板一致的925px，移动端仍截取完整文档。未合并、未部署。
- 2026-08-21，修复风险、研究、资讯或运行证据任一延迟分区失败时整页完成链被拒绝的问题；失败分区现独立显示错误，其他三个分区继续请求和渲染，页面汇总状态为`partial`且保留成功内容。未改动真实数据，未合并、未部署。
- 2026-08-14，修复金融终端真实Chrome测试服务器未为新增`.mjs`原生模块返回JavaScript MIME、导致浏览器按`application/octet-stream`拒绝动态导入的问题；服务器现统一以`text/javascript`提供`.js/.mjs`，分区探针遇到首屏启动错误会立即输出可见原因，不再等待40秒后只报告笼统超时。未合并、未部署。
- 2026-08-14，修复首轮页面热修复后360px Chrome回归发现“总览”“使用条款”“隐私政策”链接高度达到44px但宽度不足的问题；移动端分区导航与法律入口现显式满足至少44×44px触控区域，并由静态和真实浏览器契约共同守卫。未合并、未部署。
- 2026-08-14，修复DGS10观测窗口异常夹具写死`4.68`、真实日更值恰好推进到同一数值后不再形成篡改而误报资格失败的问题；夹具现相对当前快照动态偏移1bp，仍严格验证窗口末值必须与当前收益率一致，不修改真实数据、阈值或适配逻辑。未合并、未部署。
- 2026-08-14，修复每日调度器查询最近一次`workflow_dispatch`时未按分支过滤、开发分支资格运行可能让`main`误判本窗口已触发的问题；宏观雷达、跨资产、公司榜、资产榜及公司榜到资产榜的依赖判断现统一限定`GITHUB_REF_NAME`，静态治理契约同时阻止该过滤回退。未合并、未部署。
- 2026-08-13，紧急修复TradingView Mini Chart脚本误用`www.tradingview-widget.com`导致SPY、QQQ、DIA与GLD四张免费代理卡全部超时的问题：页面、许可白名单、运行证据和测试契约统一改用官方`widgets.tradingview-widget.com`主机，并显式拒绝旧主机回归；不改变ETF代理披露、8秒宿主验证、官方链接回退或零行情存储边界。随本次热修复部署。
- 2026-08-13，修复免费组件公开说明升级为逐卡宿主验证后，发布门禁仍匹配旧句式而误报来源说明缺失的问题；门禁现验证页面实际公开的逐卡失败回退承诺，并纳入稳定V1资格流程的路径触发，不放宽代理披露、来源链接、零缓存值或零演示值要求。未合并、未部署。
- 2026-08-13，修复三档Chrome机器证据沿用150毫秒快速回归超时、只能证明官方链接降级却不能观察生产8秒窗口的问题；证据模式现显式使用与真实页面相同的完整8秒注册窗口，普通快速回归仍可使用短超时但不得写入稳定V1运行证据。未合并、未部署。
- 2026-08-13，修复跨日观察自动推进周期后，离线验证器仍以首次标记时间和周期做固定断言的问题；测试现按仓库当前`requestedAt`复算同周期去重、下一周期推进与伪造日期，不改动周期标记、成功计数或21:00 UTC边界。未合并、未部署。
- 2026-08-13，修复四管道刷新接近UTC日期边界时，DGS10“独立新鲜”JavaScript夹具仍写死8月7日，导致页面正确复算为`STALE`而测试错误期待`ok`的问题；夹具现使用同批动态尝试日期与时间，不修改真实观测、过期阈值或页面逻辑。未合并、未部署。
- 2026-08-12，修复四条真实管道刷新为健康快照后，金融终端JavaScript适配测试仍写死旧`partial/degraded`状态的问题；生产快照基线现按实际文件断言，回退、错配和单源失败由独立夹具注入，避免真实数据恢复反而使最终readiness误报失败。本批仅提交开发分支，未部署。
- 2026-08-12，修复RWTC生产快照首次记录实际访问路径并恢复健康后，JavaScript夹具仍缺少路径、写死旧降级状态造成的误报；测试现在沿用同一最后已知路径并按实际健康快照断言，继续校验行情、健康和卡片三方一致。本批仅提交开发分支，未部署。
- 2026-08-12，修复远端`EIA_API_KEY`为空时RWTC只能持续保留旧值的问题：宏观雷达现在优先使用EIA API v2，密钥缺失或API失败时读取同一`RWTC`标的的EIA官方日频历史页；两条路径都失败才保留旧快照。
- EIA历史页回退使用标准库解析标题、美元/桶单位、跨年周日期、空值和最多8个递增观测点，不新增依赖，不把`CL=F`或其他标的当作备源，也不在公开页面请求或记录密钥。
- 2026-08-12，修复辅助来源首次真实刷新后验证器仍把8月8日固定时间和CNN/OFR迁移`UNKNOWN`写死的问题；现在按四份实际数据/健康快照选择验收时点，并同时覆盖迁移、已跟踪、过期、回退和失败状态。
- 官方序列、跨资产、资产榜、公司榜与经济日历的回归夹具改为由当前快照复算时间、价格、bp变化和预期状态，日更数据推进后不再因旧日期、旧价格或旧`STALE`状态误报。
- 2026-08-09，修复首轮远端刷新后金融终端验证器仍把官方历史窗口写死为旧迁移数量的问题：DGS10、DTWEXBGS与RWTC现统一验证1至8项严格递增、正数、有限且与当前值/前值一致的观测，并覆盖1点迁移、8点完整窗口与异常样例。
- JavaScript适配、来源健康、公司榜与资产榜测试改为按当前真实快照及`health.json`契约复算，不再把首次迁移的`UNKNOWN`、零行情覆盖或空领涨/领跌误当作永久条件。

### 状态

- 2026-08-21，本批远端Quality与稳定V1资格流程全部成功：四条核心真实管道完成新周期刷新，完整数据、来源健康、失败保留、工作流治理、360/768/1280像素Chrome和readiness回写通过；四条工作流均由3/7推进至4/7，剩余3个不同UTC日成功周期。最终门禁为`PASS 12 / WARN 1 / BLOCKED 0`，唯一警告是FRED `DTWEXBGS`观测超过3个美国工作日；稳定V1仍为`BLOCKED`，未合并`main`、未发布、未部署。
- 2026-08-14，本批分区加载远端验证全部通过：Quality #43、稳定V1资格 #29及辅助资格 #7（仅重跑首次失败的最终校验任务）均为成功；真实Chrome确认首屏只读取5份核心资源，点击资讯分区后按需增至9份，并在360/768/1280像素下完成全页卡片、代理宿主、官方趋势、来源健康和稳定V1证据回归。同一UTC日重复资格运行仍保持四管道各3/7，不虚增周期；未合并`main`，未部署。
- 2026-08-14，本批远端Quality #41与稳定V1资格 #27全部通过：四条核心真实管道、完整数据与来源健康、失败保留、工作流治理、360/768/1280像素真实Chrome、响应式截图、代理运行历史及最终证据提交均成功；四条工作流已各累计3/7个不同UTC日成功周期，距离稳定V1各剩4周期。当前仍为`Public Beta`，未宣称稳定V1，未合并`main`，未部署。
- 2026-08-12，本批辅助来源远端资格验收`4/4 success`：CNN 1/1、OFR 5/5、Forex Factory经济日历1/1、Google News/Yahoo资讯快照6/6组件全部为`tracked/healthy`，连续失败均为0。刷新后完整数据、失败保留、工作流治理与360/768/1280像素Chrome检查全部通过；未合并`main`，未部署。
- 2026-08-12，本批最终远端稳定V1资格验收全部成功：四条核心真实管道、完整数据与健康检查、失败保留、工作流治理、readiness生成、360/768/1280像素Chrome及诊断/截图Artifact均通过。门禁为`PASS 7 / WARN 2 / BLOCKED 4`；四条工作流各有1个不同UTC日成功周期，剩余阻塞为3/7周期证据、4项明确演示资产及EIA RWTC观测过期。未标记`READY FOR RELEASE`，未合并、未部署。
- 2026-08-12，本批代理契约、中证500回退、沪深300预防性回退及生产健康夹具修复均已独立提交；本地数据/适配器、来源健康、失败保留、工作流治理、Beta/稳定V1契约、资格编排及360/768/1280像素真实浏览器回归全部通过。无远端周期证据的本地门禁为`PASS 6 / WARN 3 / BLOCKED 0`；4项演示核心资产、RWTC观测过期和当前生产中证500旧回退仍为准确警告。当前环境没有已认证`gh`，因此尚未推送、未执行远端数据刷新和远端门禁，未合并、未部署。
- 2026-08-12，本批开发分支远端质量检查在提交`62786e5`通过；数据与适配器、逐条质量、四类来源健康、失败保留、工作流治理、两套资格编排及360/768/1280像素真实Chrome检查全部成功。基于公开Actions证据复核后的上线准备报告为`PASS 5 / WARN 4 / BLOCKED 4`；四条核心日更仍各为1/3个Beta周期、1/7个稳定V1周期，未标记`READY FOR RELEASE`，未合并、未部署。
- 2026-08-12，CNN与OFR首次远端辅助资格及修复后完整复核通过：CNN 1/1组件、OFR 5/5组件均为100%刷新覆盖并转为`tracked/healthy`；360/768/1280像素最终页面验收通过。
- 上一批完整开发分支的上线准备报告为`PASS 2 / WARN 6 / BLOCKED 4`；DGS10与DTWEXBGS通过，RWTC仍回退/过期，四条核心工作流在本分支均为1/3个Beta周期、1/7个稳定V1周期，因此继续标记`BLOCKED`，未合并、未部署。
- 首轮资格验收已在完整开发分支上成功运行宏观雷达、跨资产、公司榜与全球资产榜四条核心管道；四份健康历史已由迁移`unknown`转为真实`tracked`，同一UTC日只计1个成功周期。页面结构和数据口径未修改，尚未合并、尚未部署；稳定V1仍需累计7个不同日更周期并消除5项演示行情。

### 发布

- 2026-08-21，项目所有者批准将最新完整开发分支统一合并`main`并由GitHub Pages部署为`Public Beta`：上线版本保留4项站内真实数据、4项明确披露的TradingView免费ETF代理、0项演示数据及四管道5/7稳定V1证据。生产冒烟发现首页仍沿用旧“演示”口径后，已同步修正中英文入口并新增防回退契约；本次不宣称稳定V1，剩余2个不同UTC日周期继续累计。
- 2026-08-14，项目所有者批准将最新完整开发分支的累计成果统一合并`main`并由GitHub Pages部署为`Public Beta`：包括8张核心资产卡的4项官方/本地真实数据与4项显式TradingView ETF代理、来源健康和3/7稳定周期证据、分区导航与可读性热修复、使用条款/隐私政策、首屏分区加载及完整响应式回归。本次不宣称稳定V1，继续公开显示代理、过期、降级和剩余4个稳定周期；生产资讯快照保留`main`较新版本。
- 2026-08-12，项目所有者批准将当前完整成果作为`Beta Preview`部署到生产站点；本次不声明稳定V1，首屏继续显示`4 REAL · 4 DEMO`，三大股指与黄金仍逐卡标记`DEMO`，运行周期与数据回退警告继续公开披露。
- 2026-08-08，项目所有者明确批准将Ooglex金融终端以“抢先体验版”公开上线；页面继续醒目标注3项官方数据、5项演示数据，以及`STALE`、`UNKNOWN`和失败回退状态。
- 本次授权不代表Beta上线门禁已经通过，也不构成稳定V1声明；四条核心数据管道仍需完成至少3个连续日更周期的远端验收，后续再逐步补齐真实行情许可与稳定性证据。

### 数据

- 2026-08-13，同步线上`main`更新较新的电视频道及Google News/Yahoo资讯生产快照与健康证据；宏观雷达、跨资产、公司榜和资产榜继续保留开发分支同日稍晚的真实刷新，未回退任何核心行情、健康状态或稳定V1周期证据。未合并、未部署。
- 2026-08-12，远端Asset Tracker真实刷新确认中证500原指数`000905.SS`数据点不足时安全发布`510500.SS`人民币ETF代理，28/28项均为本轮Yahoo行情、100%新鲜且100%可验证；沪深300原指数`000300.SS`同批正常，未触发`510300.SS`预防性回退。未合并、未部署。
- 2026-08-12，沪深300跨资产记录继续优先使用Yahoo `000300.SS`原指数，并新增人民币`510300.SS`沪深300ETF价格收益率作为预防性回退；仅在原指数失败或异常时启用，代理误差、分红和费用差异保持显式披露。未改写当前生产快照，未部署。
- 2026-08-12，中证500跨资产记录保留Yahoo `000905.SS`原指数为首选；当其历史数据点不足或越过护栏时，按序回退至人民币`510500.SS`中证500ETF价格收益率，并结构化披露跟踪误差、分红和费用差异。未使用中证指数官网历史接口，未改写当前生产快照，未部署。
- 2026-08-12，比较线上`main`与完整开发分支后，仅同步`main`更新较新的AI排名、富豪、全球房价、影视、世界经济、广播和电视生产快照；宏观雷达、跨资产、公司榜、资产榜及金融终端保留开发分支更新版本，没有回退生产数据。
- 2026-08-12，同步`main`更新至8月11日的经济日历及8月12日的Google News生产快照与健康报告；开发分支其余六条数据管道仍比`main`更新，没有回退任何生产数据。
- CNN刷新为61（贪婪），OFR FSI刷新为-2.76（数据日2026-08-07）；两源均使用公开上游本轮真实结果，不补造数值。
- 同步`main`在首轮开发后产生的Google News与经济日历生产快照及对应健康报告；两条辅助来源现已由迁移`unknown`转为真实`tracked/healthy`，开发分支没有用旧文件覆盖线上新数据。
- 首轮开发分支资格运行从FRED重建DGS10与DTWEXBGS各8项官方观测；EIA RWTC本轮失败时如实保留原2项官方窗口并标记回退。三项当前可追溯窗口合计18/24，不补造缺失值。
- DGS10、DTWEXBGS与RWTC记录新增最多8项的`observations`官方观测窗口；当前旧快照只迁移可证明的1项、2项和2项观测，不补造DGS10缺失的前值日期。
- 三项生成器成功时从同一FRED/EIA响应重建最近窗口，失败时完整保留上次有效窗口；空值、倒序、重复日期、非正数或末值错配不会覆盖有效历史。
- 公开预览集成分支同步生产`main`自2026-08-02以来的89个自动数据提交；跨资产、公司榜、资产榜与宏观雷达保留8月7日至8日线上新值，不用开发分支旧快照覆盖生产数据。
- 旧版生产任务未保存逐条成功/回退路径的记录以`unknown`迁移；旧脚本明确标记为过期的记录继续保留`fallback`，静态和未上市估值保持`estimate`，等待新版远端任务建立真实运行历史。
- CNN恐慌与贪婪、OFR、Forex Factory经济日历和Google News资讯新增独立`health.json`，分别记录组件刷新、回退、不可用、最后尝试与成功、连续失败和旧快照保留；首次迁移历史明确为`unknown`，不把现有文件倒推为成功运行。
- CNN与经济日历整源失败时只更新健康证据，不覆盖旧数据；OFR全部动态来源失败时不再以新`updatedAt`重写纯旧值；Google News单板块失败时沿用同板块旧内容，五板块全失败时保留整份快照。
- 宏观雷达新增独立`health.json`，分别跟踪FRED `DGS10`、FRED `DTWEXBGS`与EIA `RWTC`的本轮成功、回退、不可用、最后尝试与成功时间、连续失败和旧值保留；首次迁移无法追溯的历史明确为`unknown`，不伪造成功次数。
- DGS10补齐与DTWEXBGS/RWTC一致的逐源更新时间和失败回退语义；Yahoo宏观面板整体不足时，三项官方序列仍可各自更新，终端优先读取DGS10逐条成功时间并继续兼容旧快照。

- `asset-tracker`、`companies`与`asset-ranking`新增独立`health.json`，机器可读地区分最近尝试、最近成功、连续整批失败、保留快照与发布覆盖；健康文件更新不再要求覆盖最后有效行情快照，不存在旧快照时也不会声称已经保留。
- 三条管道登记实际恢复顺序与来源角色；Yahoo备用域名明确为同提供方镜像而非独立供应商，CoinGecko、公司榜上游、静态汇率、代理代码和公开估值保持原有降级口径。
- 首次健康迁移无法追溯此前连续失败次数时保存为`null`，下一次成功归零、整批失败从1开始累计，不以文件更新时间倒推历史运行结果。
- 跨资产28项记录新增逐条 `dataMeta`：区分Yahoo本轮行情、历史回退、异常值与不可用状态，并保留逐条来源、数据日、更新时间和日频口径；文件级 `dataQuality` 可由逐条状态复算。
- 旧跨资产快照中已知的1项历史回退不再沿用文件日期冒充逐条数据日，精确日期无法追溯时明确留空并标记 `PARTIAL`。
- 公司榜500项新增相同逐条契约：上市公司区分Yahoo本轮行情与历史回退，50家未上市公司标为不定期公开融资估值；KRW换汇使用静态兜底时整条记录降级为回退状态。
- 现有450家上市公司旧快照无法逐只证明本轮成功或回退，迁移后统一标记“待确认”，金融终端暂停使用它们生成“今日领涨/领跌”；下一次自动任务成功后会写入精确状态。
- 全球资产榜250项接入同一逐条契约，并透传公司榜状态：当前旧快照中16项可确认是静态估值，234项因无法证明本轮Yahoo、CoinGecko或历史回退路径而标记“待确认”。
- 房地产、政府债券、黄金等已有来源名称被结构化；原报告日期未被旧脚本保存的项目不再以每日文件时间冒充估值日期。资产排行每日任务会在提交前校验逐条来源、频率、时间和文件级汇总。
- 金融终端的美国10年期国债收益率卡片现复用宏观雷达中的FRED `DGS10` 日频数据，不建立第二套取数管道。
- 收益率以百分比显示，相对上一观测值的变化以基点（bp）显示；同时展示FRED来源、数据日期和文件更新时间。
- 超过3个美国工作日未更新时保留最后观测值并标记为过期；加载失败、字段缺失或格式无效时隐藏数值并显示错误状态。
- 金融终端的美元卡片使用美联储广义美元指数 `DTWEXBGS`，不再把该口径显示为ICE DXY。
- `DTWEXBGS`现复用宏观雷达每日运行的FRED官方API流程，通过 `referenceSeries.DTWEXBGS` 自动更新当前值、前值、涨跌幅、数据日期和成功更新时间。
- FRED更新失败时保留上一份有效美元指数数据，单独记录失败尝试时间并标记为过期；没有历史有效值时隐藏数值并标记错误。
- 金融终端的原油卡片改为EIA“Cushing, OK WTI Spot Price FOB”日频现货序列 `RWTC`，不再使用演示值，也不以 `CL=F` 近月期货冒充现货。
- `RWTC`通过宏观雷达任务的EIA API v2流程读取最近两个有效观测值，按前值计算涨跌幅；超过4个美国工作日未发布新观测值时标记为过期。
- EIA更新失败时保留上一份有效WTI现货数据与成功更新时间，单独记录失败尝试；没有历史有效值时隐藏数值并标记错误。

### 修改

- 2026-08-21，稳定V1证据快照支持“最新完整开发分支”迁移：四管线已验证UTC日与新分支成功日取去重并集，同一UTC日重跑保持`5/7`，只有新的独立UTC日才能推进到`6/7`；当前运行结论仍来自新分支，失败不会被历史成功证据掩盖。仓库契约同时禁止低于已发布`5/7`，不改生产、不部署。
- 2026-08-21，金融终端质量工作流在Chrome三档截图前检查中文字体，仅在Runner缺失时安装Noto CJK；字体只服务CI验收图，不加入生产资源、不改变页面外部依赖，避免360/768/1280真实截图因测试机缺字显示方框。未合并、未部署。
- 2026-08-21，金融终端360px遥测区将单列轨道由`1fr`收紧为`minmax(0, 1fr)`，并为来源/更新时间子项设置`min-width: 0`；长来源串继续保留在DOM与标题语义中，但不再把文档从360px撑宽到852px。未合并、未部署。
- 2026-08-21，金融终端科幻视觉层修复360px回归发现的移动端边界：恢复分区导航44×44px触控目标，并让终端主层、全息轨道、风险地图和管线HUD的网格子项允许安全收缩；Chrome失败仍保留当档截图，并输出视口、文档宽度和前20个越界元素，避免用裁切掩盖横向溢出。未合并、未部署。
- 2026-08-21，金融终端回归契约新增科幻视觉数据校验：离线复算七区域日回报压力代理、代理标的透传、缺失隔离、四管线最小连续周期、`5/7→7/7`动态推进与过期证据保留；三档Chrome探针同步检查8项行情带、4个市场时钟、3个风险HUD、7行区域明细和动态稳定V1环。视觉模块纳入16KB、样式纳入28KB、常规JavaScript继续受230KB总预算限制。未合并、未部署。
- 2026-08-21，金融终端将宏观雷达、跨资产、公司榜和资产榜共用的“稳定V1运行证据”视图拆为第四个原生按需模块；首屏和只打开其他分区时保持`idle`，滚动接近或点击该分区后才导入并渲染四张卡。生产入口`app.js`由195,262字节降至188,846字节，常规入口加加载器由209,069字节降至202,653字节；9KB模块预算、离线空态、资讯→运行证据5→9→14份资源复用、四模块三档Chrome和资格工作流触发契约同步守卫。未改行情、健康或readiness JSON，未合并、未部署。
- 2026-08-21，金融终端将市场状态、市场研究与事件资讯三组屏下视图分别拆为原生按需模块；首屏和未打开分区不请求对应代码，模块失败继续只降级自身分区。生产入口`app.js`由215,878字节降至195,262字节，常规入口加加载器合计209,069字节；离线契约验证异步渲染屏障与三模块空态，Chrome契约验证首屏零模块请求、单分区隔离和三档完整加载。未改行情或健康JSON，未合并、未部署。
- 2026-08-21，金融终端先完成关键市场卡片渲染并跨越双动画帧绘制边界，再启动13份延迟来源的分区调度；第三方组件宿主监控保持并行，首屏仍只读取1份配置与5份关键来源，不新增依赖、数据源或固定毫秒性能结论。未合并、未部署。
- 2026-08-14，金融终端完成首轮原生模块与分区加载优化：生产入口`app.js`从229,081字节降至215,878字节，并以220KB单文件、230KB常规加载总量作为机器性能预算；请求缓存和加载调度拆至独立ES模块，首屏从一次并发18份资源改为先读取5份核心资源，另外13份按视口或分区导航延迟加载且跨分区不重复请求。12.9KB浏览器回归探针只在`regression=1`时加载，真实回归仍强制渲染全页。新增成功、HTTP失败、请求复用、导航/视口触发与5→18资源阶段契约；未合并、未部署。
- 2026-08-14，金融终端完成首轮可读性与长页热修复：密集辅助信息计算字号统一不低于11px；顶部由笼统`STALE DATA`改为动态“正常／降级／过期／不可用／提供方代理”数量；新增7项粘性分区导航；完整数据方法说明改为摘要加原生可展开详情；页脚版本名统一为`Public Beta`。三档响应式、键盘焦点与触控目标契约同步覆盖。未合并、未部署。
- 2026-08-13，项目所有者将四项待授权行情正式锁定为精确原标的：`SPX`、`NDX`、`DJIA`与`LBMA Gold Price PM (USD/oz)`；使用范围限定为个人爱好者、无广告/订阅/其他收入、日频或延迟公开网页展示，SPY/QQQ/DIA/GLD均明确为未选择。机器门禁新增询价准备、提交编号、报价与授权状态交叉校验；当前4项继续`blocked / keep-demo`，未合并、未部署。
- 2026-08-12，稳定V1资格工作流新增资产取数、逐条质量、来源健康、资产工作流和金融终端适配契约触发路径；核心数据链或适配测试变化会自动执行四管道真实刷新与最终readiness，不再只在编排脚本变化时运行。未合并、未部署。
- 2026-08-12，聚合来源健康与稳定V1门禁把动态行情、已登记市值代理和慢频估值分层：公司榜50项融资估值、资产榜15项公开估值单独显示慢频覆盖，不再因合法`estimate/irregular`记录误报行情失败；动态回退、未知、不可用和未登记`partial`路径仍保持降级。未部署。
- 三张官方行情卡片新增`RECENT OBSERVATIONS`区域，显示最近观测点数、起止日期和区间变化；DGS10使用bp，DTWEXBGS与RWTC使用百分比，历史不足时明确提示等待官方刷新补足。
- 官方观测趋势适配器交叉校验最多8项递增日期、正数值及窗口末项与当前记录的一致性；三源错误互相隔离，不使用演示走势填充官方卡片。
- 金融终端回归验证改为按当前`dataQuality`动态校验逐条计数、来源标签和覆盖率，同时覆盖“接下来事件”与“最近事件”的不同排序；日更数据变化不再因旧快照硬编码造成误报。
- 全球资产方法说明不再写死旧快照的16/234计数，改由当前`data.json`动态汇总行情、回退、估值和待确认覆盖。
- DGS10、DTWEXBGS与RWTC三张官方行情卡片新增逐源`UPDATE HEALTH`，分别显示本轮刷新或回退、连续失败、最近尝试和最后成功；行情观测过期与自动更新链健康保持独立，不再只能从页面底部的聚合状态判断单项来源。
- 官方逐源健康适配会交叉校验来源、序列ID、观测日和发布成功时间；单项健康错配只把对应健康栏降为`UNKNOWN`，不隐藏仍可验证的行情，也不拖累另外两项。健康证据超过72小时显示`STALE`，迁移历史继续标为待建立。
- 金融终端在CNN、OFR、经济日历和财经新闻现有卡片中新增`UPDATE HEALTH`，分别显示可展示覆盖、本轮刷新、连续失败和最近尝试；更新链失败或超时不会被卡片描述为正常运行，页面卡片数量与布局不变。
- 四条辅助任务接入独立并发锁、生成前分支同步、精确`data.json`/`health.json`路径守卫、无变化退出和安全推送重试；失败健康证据提交后任务再以失败结论结束，诊断只保留14天Artifact。
- 金融终端新增四管道运行状态适配与渲染：同时交叉校验`macro-radar`、`asset-tracker`、`companies`和`asset-ranking`的数据/健康快照，分开展示可用、本轮新鲜、已验证覆盖、最近尝试与成功、连续失败及旧快照保留；单源文件失败不会拖累其他管道。
- 响应式回归扩展为8/3/3/2/4张卡片，四管道状态在360、768、1280像素分别使用1、2、4列；Beta门禁与反馈入口纳入键盘、外链安全和手机44像素触控检查。
- `macro-radar`日更任务接入独立并发锁、生成前分支同步、精确输出路径守卫、无变化退出和安全推送重试；只允许提交`data.json`、`health.json`与`history.json`，逐次诊断仅保留14天Artifact。
- Beta门禁新增宏观逐源健康检查，区分“最后观测仍可展示”和“最近更新任务失败”；三源全失败、健康错配或报告超过72小时直接`BLOCKED`，部分回退与迁移历史未知为`WARN`。

- 金融终端只读质量CI新增上线门禁契约测试；门禁脚本、验证器和工作流变化均会触发开发分支质量检查，生产页面、行情JSON和既有数据计算不变。
- `asset-tracker`日更任务新增单管道并发锁、生成前分支同步和精确输出路径守卫；同一任务不会重叠运行，越权修改会在提交前失败，无文件变化时跳过提交与推送。
- `companies`日更任务接入同一治理链；原先对整个应用目录的宽范围暂存已收紧为`data.json`、`health.json`及`logos/`根目录PNG，新Logo仍可自动加入，页面与非图片文件不会被数据任务带入提交。
- `asset-ranking`日更任务接入同一治理链，并在生成前同步公司榜最新成功提交；资产总榜只拥有自身`data.json`与`health.json`，不会反向提交公司榜上游文件。

- 跨资产五周期选择器改为标准ARIA标签页，支持左右/上下方向键循环、Home/End跳转及自动激活，并保持鼠标和触屏点击方式不变。
- 金融终端将低对比辅助文字提升至WCAG AA对比度，手机端主要控件提供至少44像素触控高度，并补齐长文本换行、减少动画、高对比与系统强制颜色规则。

- 全球市场总览采用逐资产状态：`DGS10`与`DTWEXBGS`读取FRED数据，`RWTC`读取EIA现货数据，其余5项继续逐卡标注为`DEMO`。
- 页面首屏、数据说明、SEO描述和无障碍文本同步改为“3项官方数据 + 5项演示数据”的准确口径。
- 金融终端校验脚本覆盖DGS10、DTWEXBGS与RWTC的正常、自动更新失败回退、过期、缺失、无效、错误来源和数据文件请求失败状态。

### 安全

- 宏观时光机的FRED历史请求改为参数对象传递密钥，异常日志只输出错误类型和HTTP状态，不再把密钥拼入请求URL或异常文本。

- Beta门禁工作流权限限制为`actions: read`与`contents: read`，不读取FRED/EIA等行情Secrets；GitHub临时令牌只经请求头使用，不进入参数、报告或日志，工作流不提交数据、不推送代码且不触发部署。
- FRED请求密钥改为通过请求参数对象传递；异常日志只记录错误类型和HTTP状态，不输出可能包含密钥的完整请求URL。
- EIA API密钥只从GitHub Actions的 `EIA_API_KEY` Secret注入并通过请求参数对象传递；代码、前端、数据文件和错误日志均不保存或输出密钥。

### 文档

- 2026-08-13，新增精确原标的授权询价包，按S&P DJI（SPX/DJIA）、Nasdaq（NDX）与ICE IBA（LBMA Gold Price PM）分别登记官方入口、联系地址、非商业延迟展示范围、英文询价文本、提交审计字段和授权后工程验收；材料明确为“已准备、未提交”，不在公开仓库保存姓名、邮箱、电话、合同或付款资料。未合并、未部署。
- 更新宏观官方序列规范与路线图，记录最近观测窗口、单位复算、失败保留、旧快照迁移和页面提示；本批仅提交开发分支，未合并、未部署。
- 更新宏观逐源健康规范与路线图，说明三张官方行情卡片的独立更新链展示、72小时时效和单源故障隔离；未修改任何行情或健康快照，未部署。
- 新增`docs/SUPPORTING_SOURCE_HEALTH.md`，说明四个辅助来源的组件、12/36/72小时任务阈值、逐源回退、路径所有权、14天诊断和不扩大Beta门禁的边界。
- 更新路线图，要求四个辅助来源至少完成一次远端真实运行以消除迁移`UNKNOWN`，同时继续等待四条核心管道连续3个日更周期。
- 新增`docs/FINANCE_TERMINAL_OPERATIONS_RUNBOOK.md`，统一四条生产管道的日常验收、远端运行顺序、状态分诊、逐管道恢复、14天证据、可审阅回退和3/7周期关闭标准。
- 更新路线图与上线门禁规范，明确页面健康快照不包含远端Actions历史，不能替代Beta连续周期证据。
- 新增`docs/MACRO_SOURCE_HEALTH.md`，说明三项官方序列的独立状态、3/3/4工作日阈值、恢复顺序、路径所有权与门禁语义。

- 新增`docs/FINANCE_TERMINAL_RELEASE_GATE.md`，定义四条生产数据管道、3/7个日更周期、72小时健康时效、演示/许可边界及Beta与稳定V1的不同放行标准。
- 新增`docs/AGGREGATE_SOURCE_HEALTH.md`，说明三条聚合管道的来源、频率、条款核查状态、发布门槛、恢复顺序和健康字段语义。
- 新增 `docs/MARKET_DATA_SOURCES.md`，为全球市场总览的8项资产明确目标标的、候选真实来源、更新频率、变化口径、过期阈值、许可门槛和故障回退流程。
- 明确三大美国股票指数、ICE DXY和LBMA黄金不能因“技术上能取到”就直接公开展示；许可未确认前继续保留演示数据。
- 明确美国10年期国债收益率使用bp显示变化，且现货、期货、指数和ETF不得静默互相替代。

### 新增

- 新增辅助来源健康契约、跨文件验证器和离线生成器测试，覆盖全成功、部分回退、整源失败、连续失败、时间错配、覆盖率篡改及旧快照不被重写。
- 金融终端新增“Beta数据运行状态”区域，四张卡分别展示宏观官方序列、跨资产强弱、全球公司榜和全球资产榜的最近健康快照；超过72小时统一显示`STALE`，契约或快照错配显示`UNKNOWN`。
- 新增结构化金融终端数据问题Issue表单和页面反馈入口，固定选择四条生产管道、问题类型、页面、发现时间、实际结果与公开核对依据，并禁止提交密钥、令牌、账号或持仓信息。
- 新增金融终端Beta上线门禁：只读核对DGS10、DTWEXBGS、RWTC的来源/单位/涨跌/时效，三条聚合快照与健康契约，以及四条GitHub Actions在目标分支上的远端日更证据；同时输出机器可读JSON与人工可读Markdown。
- 新增Beta与稳定V1双目标结论：Beta在无`BLOCKED`时可带明确演示数据`WARN`上线，稳定V1要求零演示数据、全部本地检查`PASS`及四条工作流连续成功7个周期。
- 新增只读`Finance Terminal Beta Gate`工作流，每日及手动生成上线准备报告并保留14天Artifact；同一调度周期的重跑按最终结果去重，不以重复触发虚增成功周期。
- 新增聚合数据工作流治理脚本与契约测试，统一检查路径所有权、越权阻断、空差异退出、分支引用、并发锁和安全推送重试；`asset-tracker`运行诊断以14天Artifact保留，不新增诊断历史文件到Git。
- 三条聚合管道的来源健康与路径诊断统一保留为14天GitHub Actions Artifact；仓库继续只保存页面所需的最新数据和健康快照，未新增逐次诊断历史。

- 金融终端三张“市场强弱与领袖”卡片新增来源健康栏，显示管道状态、本轮行情覆盖、最近尝试、连续失败和最后成功时间；健康报告超过日频管道72小时时明确标记`STALE`，健康文件缺失或错配只降级健康栏，不隐藏仍可验证的行情快照。
- 金融终端只读质量工作流新增三源健康汇总JSON并保留14天，诊断产物不进入网站、不读取Secrets且不触发部署。
- 金融终端新增“跳到主要内容”入口与集中式读屏状态播报；四组异步卡片使用列表语义，加载完成后一次性说明异常数量。
- 新增无第三方前端依赖的金融终端Chrome回归脚本与只读GitHub Actions：在360、768、1280像素检查真实数据加载、卡片数量、响应式列数、横向溢出、Tab顺序、周期方向键、触控目标、外链安全和截图。

- 新增 `/apps/finance-terminal/` 全球市场总览MVP，以明确标注的演示数据展示标普500、纳斯达克100、道琼斯指数、美国10年期国债收益率、美元指数、黄金、原油和比特币。
- 新增金融终端首页入口，并保留宏观雷达和全部现有栏目入口。
- 新增无第三方依赖的金融终端数据与页面校验脚本。
- 金融终端新增“市场状态”模块，首项复用宏观雷达已有的0–100制度信号分数、状态说明、来源、数据日期、更新时间与失败状态，不另造综合风险算法。
- “市场状态”模块新增CNN恐慌与贪婪指数，展示0–100读数、中文评级、相对上一收盘变化、数据日期、更新时间与独立过期/错误状态。
- “市场状态”模块新增OFR金融压力指数，展示当前读数、较前日变化及相对历史平均压力的解释；缺少变化值、过期和加载失败分别显示`PARTIAL`、`STALE`和`ERROR`。
- 金融终端新增“跨资产强弱”卡片，复用全球资产表现的28项日更数据，支持今日、近一周、近一月、年初至今和近一年领涨/领跌切换；过期、可疑和缺失值不参与排行，单源失败不影响其他模块。
- 金融终端新增“全球资产市值”卡片，复用250项跨品类市值榜并展示前五名与样本合计；页面区分慢变量公开估算和随行情更新项目，不把混合频率数据标成实时。
- 金融终端新增“全球公司领袖”卡片，复用500家公司市值数据，展示上市公司市值前三及当日领涨、领跌；50家未上市公司估值不参与涨跌排序，逐公司回退状态缺失时明确显示`PARTIAL`。
- 金融终端新增“重要经济事件”卡片，复用Forex Factory公开周历，展示接下来最多4项高、中影响事件以及实际、预测和前值；时间按设备时区显示。
- 经济日历独立校验来源、事件数量、影响级别、周范围和更新时间；超过36小时未更新或离开当前周时显示`STALE`，单源失败时不影响行情、风险和排行模块。
- 金融终端新增“最新财经新闻”卡片，只读取现有新闻聚合中的市场板块，按发布时间展示最新5条标题、原媒体和本地时间；每条链接经Google News跳转原媒体。
- 财经新闻独立校验来源、板块、标题、发布时间和安全链接；文件超过12小时未更新或最新文章超过36小时时显示`STALE`，单源失败不影响经济日历及其他模块。

### 状态

- 本批三项官方趋势模块仅在开发分支实现；除新增可追溯观测窗口外，未改动现有价格、涨跌、来源、更新时间或健康状态，未合并`main`、未部署。
- 本批仅复用现有宏观`data.json`与`health.json`；DGS10、DTWEXBGS和RWTC卡片当前均如实显示过期健康证据及迁移历史待建立，未接入新API、未修改数值、未部署。
- 本批四个辅助来源均为迁移`UNKNOWN`：现有内容可展示，但尚无本分支远端真实运行证据；未修改任何既有行情、风险、日历或新闻数值，未扩大公开Beta门禁，未部署。
- 本批仅在开发分支实现并验证，未合并`main`、未部署；当前页面四条管道均因健康报告超过72小时显示`STALE`，Beta门禁仍应为`BLOCKED`。
- 当前宏观逐源健康为迁移`unknown`且已超过72小时，三条聚合健康报告也已过期；在远端完成真实更新并建立连续3个成功日更周期前，Beta门禁继续为`BLOCKED`。

- 当前本地门禁结论为`BLOCKED`：三条聚合健康报告已超过72小时，三项官方行情观测过期，且开发分支尚未建立四条工作流连续3个远端日更周期；5项演示资产单独记录为Beta `WARN`而非伪装成真实行情。
- 当前页面不在浏览器中直连外部API；DGS10、DTWEXBGS与RWTC读取仓库内宏观雷达已生成的数据文件，其余5项仍为合成演示数据。
- 已扩展宏观雷达取数脚本与数据文件，但不改变宏观雷达页面现有模块；现有GitHub Actions继续负责每日行情更新，无新增行情取数工作流或依赖。本批新增的门禁工作流只读检查运行证据。
- 经济日历卡片只读取现有 `apps/econ-calendar/data.json`，未修改其取数脚本、数据文件或工作流。
- 财经新闻卡片只读取现有 `apps/whats-latest/data.json` 的 `markets` 板块；同文件中的Yahoo行情快照不参与终端行情，未修改上游取数、数据或工作流。
- 本地未配置 `EIA_API_KEY`，因此提交中的RWTC初始保留值来自EIA官方历史页并显示为 `STALE`；自动刷新需在合并后由已有工作流读取仓库Secret。
- 未部署生产环境。

## [2026-08-03]

### 文档

- 新增 `docs/PROJECT_BRIEF.md`，明确Ooglex AI金融终端的定位、用户、功能范围和数据可信标准。
- 新增 `docs/ROADMAP.md`，将项目拆分为MVP、数据质量、Cloudflare、AI研究、自选股与运营阶段。
- 新增根目录 `AGENTS.md`，固化数据真实性、密钥安全、移动端、测试和生产部署规则。
- 新增本更新日志，作为以后每日开发记录的统一入口。

### 状态

- 未开发具体业务功能。
- 未修改现有页面、数据脚本和GitHub Actions。
- 未部署生产环境。
